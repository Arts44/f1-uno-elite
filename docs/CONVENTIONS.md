# Conventions du projet — F1 UNO Élite · Collection Tracker

> **Read me first / À lire d'abord.** This file (`docs/CONVENTIONS.md`) is the operating manual for any AI model working in this repo. It exists so a model can produce work indistinguishable from the maintainer's own, **without** the maintainer filling gaps from memory. It is **bilingual**: each section gives the rule in **English**, then a short **🇫🇷 FR** mirror for the parts that matter most. When English and French disagree, **English wins** (the code, filenames and commit history are English).
>
> **Ce fichier fait autorité.** Si tu ne trouves pas la réponse ici, lis le `README.md` (source de vérité détaillée), puis demande — ne devine pas sur les règles marquées **HARD RULE**.

---

## 1. What this project is / Ce que c'est

**F1 UNO Élite — Collection Tracker** is a 100 % client-side single-page PWA that lets a collector track their *F1 UNO Élite* trading-card collection: owned cards, doubles, wishlist, favourites, badges, titles and completion stats. **No backend, no database, no framework, no runtime dependencies.** State lives in the browser's `localStorage`; the app works fully offline once installed.

- **Live app:** https://arts44.dev/ (GitHub Pages via the `CNAME`, served static from `main`).
  ⚠ **Never publish or link the old `arts44.github.io/f1-uno-elite/` address.** It answers **301**, and **a service worker does not follow a redirect** — a PWA installed on the old origin freezes permanently on its cached shell, with no update path. See `docs/POINTS-SIGNALES.md` n°18 (measured on a real install, 13/08).
- **Repo:** `Arts44/f1-uno-elite` — the git root is the `Collections/F1/` folder, **not** the parent `ARC/` folder. All commands below assume you are in `Collections/F1/`.

🇫🇷 App 100 % côté client, hors-ligne, sans serveur ni framework. Le dépôt git est le dossier `Collections/F1/`. Toute progression est stockée dans `localStorage`.

---

## 2. HARD RULES — non-negotiable / Règles absolues

These are the rules that, if broken, ship a bug to production or violate the project's core promises. Treat every one as a blocker.

1. **Zero runtime dependencies. HARD RULE.** No npm packages, no CDN `<script>`/`<link>`, no framework, no SDK at runtime. The *only* dependency is `esbuild`, a `devDependency` used to bundle. Cloud calls use raw `fetch()` (no Supabase SDK). QR uses the vendored `qrcodegen.js`. Fonts are self-hosted WOFF2 — **never** add a Google Fonts request. If a task seems to need a library, stop and ask.
   🇫🇷 Aucune dépendance à l'exécution. Jamais de CDN, jamais de SDK. Seul `esbuild` (devDependency) est permis. Si une lib semble nécessaire → demander.

2. **All URLs must be relative. HARD RULE.** GitHub Pages serves from a sub-path (`/f1-uno-elite/`). Nothing may start with `/`. Every `fetch()`, asset href, `manifest.webmanifest` `start_url`/`scope`, and the service-worker registration/precache list uses `./` relative paths so the app runs identically at localhost, a domain root, and a sub-path. A leading-slash path is a bug.
   🇫🇷 URLs toujours relatives (`./`). Un chemin commençant par `/` casse GitHub Pages → c'est un bug.

3. **Rebuild the bundle after touching a bundled JS source. HARD RULE.** `app.bundle.js` (+ `.map`) is a **committed** artifact because Pages runs no build step. After editing any *bundled* root module, run `npm run build` and commit the regenerated bundle. The **bundled** sources are every root `*.js` **except** these classic (non-module) scripts, which are *not* bundled and need **no** rebuild: `app/sw.js`, `translations.js`, `data-embedded.js`, `card-descriptions.js`, `cloud-config.js` (and `app.bundle.js` itself). A local `.git/hooks/pre-commit` automates the rebuild, **but git hooks are not versioned** — after a fresh clone the hook is gone; recreate it or rebuild manually. Never commit a JS source change without the matching bundle.

   **UN ARTEFACT GÉNÉRÉ *ET* COMMITÉ PORTE L'INSTANT OÙ IL A ÉTÉ CONSTRUIT, DONC UN ÉTAT DE PLUS QUE SES SOURCES.** Ce n'est pas la même chose qu'un fichier dérivé ordinaire : `app.bundle.js` fige l'arbre de travail tel qu'il était au moment du `npm run build` — **modifications non commitées comprises**. Deux sessions au même commit source produisent le même bundle ; deux sessions dont l'une a des sources modifiées non commitées n'en produisent pas le même. **Vécu le 22/08/2026** : deux sessions Claude Code travaillaient sur ce dépôt, l'une avec `app/changelog.js` et `app/sw.js` modifiés et non commités. Un `npm run build` lancé depuis l'autre aurait produit un bundle contenant ce travail en cours et l'aurait commité sans que personne ne l'ait lu — et git n'aurait montré qu'un conflit sur une ligne minifiée de 300 Ko, illisible et non résoluble à la main, où le dernier `git add app/` gagne en silence. **LA RÈGLE : le bundle ne se commite QUE dans le commit qui modifie ses sources, et jamais par une session qui n'a pas écrit ces sources.** Le hook `pre-commit` installé par `scripts/installer-hooks.sh` le refuse, et **il lit la liste des sources dans le sourcemap du bundle qu'on commite** plutôt que dans une liste tenue à la main — une liste écrite dans le hook serait une copie (㉘) qui laisserait passer le premier module ajouté sans elle.
   🇫🇷 Après édition d'un module *bundlé*, `npm run build` puis commit du bundle régénéré. Non-bundlés (pas de rebuild) : `app/sw.js`, `translations.js`, `data-embedded.js`, `card-descriptions.js`, `cloud-config.js`. Le hook pre-commit n'est pas versionné → le recréer après un clone.

4. **Bump `SW_VERSION` when any precached shell file changes. HARD RULE.** The service worker (`app/sw.js`) precaches the app shell **cache-first**. If you change any precached file (HTML, CSS, a bundled JS, a `data/*.json`, an icon, a font…) without bumping `SW_VERSION`, returning users keep the **stale** cached version. `SW_VERSION` is a single line near the top of `app/sw.js`. Bump it (e.g. `v37` → `v38`) on any release that alters a precached file, and confirm the precache list still lists every shipped shell file. **The new SW must stay in `waiting`** — never re-add `skipWaiting()` to the install handler: the in-app update banner (`update.js`) promotes it via the `SKIP_WAITING` message; auto-promotion would swap the shell under a running app. For a **user-visible release**, also add a `CHANGELOG` entry in `changelog.js` (that IS the app-version bump — `APP_VERSION` derives from the newest entry), with the change texts in all 7 languages.
   🇫🇷 **Toute modif d'un fichier du shell précaché ⇒ incrémenter `SW_VERSION` dans `app/sw.js`. Ne jamais remettre `skipWaiting()` auto dans `install` (le bandeau de mise à jour le déclenche via message). Release visible ⇒ ajouter une entrée `CHANGELOG` dans `changelog.js` (c'est le bump de version), traduite ×7. 🇫🇷 **« Release VISIBLE » veut dire visible PAR LE LECTEUR DU CHANGELOG, pas visible en général.** Le changelog est celui de l'APP, lu dans « Nouveautés » par des gens qui n'ouvrent jamais la vitrine : y annoncer un correctif de vitrine — décalage de mise en page, poids de la page d'accueil — serait une ligne fausse dans un fichier dont toute la valeur est d'être vrai. Un correctif de vitrine n'y entre pas, et n'entraîne donc pas de bump de version. Même raisonnement pour tout ce que l'utilisateur ne peut pas observer depuis l'app.
   ⚠ **Un correctif inobservable depuis l'app y entre quand même s'il concerne un ARTEFACT DÉJÀ SORTI.** Le critère n'est pas seulement « l'utilisateur peut-il l'observer », c'est aussi « des utilisateurs détiennent-ils quelque chose de faux ». Tranché le 22/08/2026 sur la carte de profil : l'adresse imprimée en pied des cartes partagées avant 1.77.3 ne mène plus à l'app, et **le changelog est le seul canal vers les gens qui ont cet artefact en circulation**. Corriger sans le dire les laisse avec un lien mort qu'ils croient bon. Voir POINTS-SIGNALES n°39.
   ⚠ **Two `sw.js` exist and they are not interchangeable.** `app/sw.js` is the real service worker — it holds `SW_VERSION` and the precache list. The root `sw.js` is the **demolition SW** kept since the move to `/app/` (1.62.0): it unregisters itself for PWAs installed back when the app lived at the root, so they do not freeze the way n°18 describes. Never bump one thinking you bumped the other.

5. **UI text goes through i18n, in all 7 languages. HARD RULE.** Never hard-code user-facing text. Add a key and call `t('my.key')` (JS) or `data-i18n="my.key"` (HTML). Every new key must be added for **all 7 languages** in `translations.js`: `en, fr, es, zh, it, nl, de`. English is the fallback; an English-only string is an incomplete change.
   🇫🇷 Aucun texte d'interface en dur. Ajouter la clé dans `translations.js` pour **les 7 langues** (`en, fr, es, zh, it, nl, de`) et utiliser `t()` / `data-i18n`.

6. **Debug logging only through `logger.js`. HARD RULE.** Use `log()` / `warn()` from `logger.js`; they are gated behind `export const DEBUG = false` and are **no-ops in production**. Never leave raw `console.log` for debugging. Genuine, always-on error reporting (`console.error`, and `console.warn` for real warnings) is fine and expected. Ship with `DEBUG = false`.
   🇫🇷 Debug via `log()`/`warn()` de `logger.js` (no-op en prod, `DEBUG=false`). Jamais de `console.log` de debug résiduel. `console.error` pour les vraies erreurs = OK.

7. **Cloud config: anon key only, never `service_role`. HARD RULE.** `cloud-config.js` holds the Supabase `url` + `anon` public key. These are public by design (protected by Row Level Security server-side). **Never** put the `service_role` key anywhere in the repo — it bypasses RLS. `cloud-config.js` is a classic script (not bundled): editing it needs no rebuild. Empty both values to fully disable cloud.
   🇫🇷 `cloud-config.js` : uniquement l'`url` + la clé `anon` (publiques, protégées par RLS). **Jamais** la clé `service_role` dans le repo.

8. **Serve over HTTP — never `file://`. HARD RULE (for running/testing).** ES-module loading and `fetch()` of `data/*.json` are both blocked over `file://`. Always serve the folder over a local HTTP server before testing.
   🇫🇷 Toujours servir en HTTP local ; `file://` casse les modules ES et le `fetch` des JSON.

9. **Vider le cache AVANT de mesurer quoi que ce soit dans le navigateur. HARD RULE (pour vérifier).** Il y a **TROIS pièges**, et ils se découvrent un par un — cette règle a dû être corrigée trois fois :

   | Piège | Ce qui sert du périmé | Ce qui le perce |
   |---|---|---|
   | 1. Service worker | tout le shell précaché | `registration.unregister()` + `caches.delete()` sur chaque clé |
   | 2. Cache HTTP du **document** | `index.html` | rechargement avec une **URL modifiée** (`?v=<n>`) — `location.reload()` ne suffit pas |
   | 3. Cache HTTP des **sous-ressources** | `styles.css`, `app.bundle.js`, `translations.js`… | `fetch(f, {cache:'reload'})` sur **chacune** — changer l'URL du document ne les touche pas, elles ont leur propre entrée |

   La recette complète, dans cet ordre : **purger SW + `caches` → `fetch(…,{cache:'reload'})` sur les sous-ressources modifiées → recharger avec une URL modifiée.**

   **Le piège 1 ne s'arrête pas aux fichiers du dépôt.** Le service worker intercepte **toute** navigation dans sa portée (`./`) — y compris un fichier que tu viens de créer et qui n'a **jamais** existé auparavant. Une page de maquette écrite il y a deux minutes a été servie périmée : le SW répondait à sa place, et la page affichait une version antérieure d'elle-même. Le symptôme est déroutant (« mon `window.go` n'existe pas alors qu'il est dans le fichier ») et n'évoque pas le cache. **Avant de servir une page de travail temporaire, purge le SW** — ou sers-la depuis un autre port, hors de la portée.

   Pourquoi c'est une règle et pas un conseil : plusieurs mesures ont été prises sur du code périmé avant qu'on s'en aperçoive, et des conclusions ont dû être jetées. Une mesure faite sur un bundle obsolète ne se voit pas — elle a l'air juste.

   **Corollaire pour les mesures de mise en page :** `requestAnimationFrame` **ne se déclenche pas** dans un onglet ou un panneau masqué. Toute attente de stabilisation qui repose dessus ne stabilise rien et renvoie des valeurs oscillantes. Utiliser `setTimeout` seul.

   🇫🇷 Deux caches à purger : le SW **et** le cache HTTP. Purge SW + `caches`, puis rechargement avec URL modifiée. Et pas de `requestAnimationFrame` pour attendre la mise en page : il ne tourne pas dans un panneau masqué.

---

## 3. Stack & architecture

| Area | Choice |
|---|---|
| Language | Vanilla JavaScript (ES modules, ES6+), HTML5, CSS3 — no framework |
| Entry point | `app.js` (ES module): init sequence, global event wiring, SW registration |
| Build | esbuild → `app.bundle.js` (IIFE, minified, sourcemap). `npm run build` / `npm run dev` (watch) |
| Runtime deps | **None** |
| Storage | Browser `localStorage`, season-scoped keys |
| PWA / offline | `app/sw.js` (versioned precache, cache-first shell, stale-while-revalidate for external images) + `manifest.webmanifest` |
| Crypto | Web Crypto `SHA-256` (PIN); `CompressionStream` (backup codes) |
| QR | Vendored `qrcodegen.js` (Project Nayuki, MIT) — byte mode + SVG |
| Cloud (optional) | Supabase via **raw REST `fetch()`** (GoTrue magic-link auth); gated on `cloud-config.js` being filled |
| Tests | Node's built-in runner: `node --test` (no test framework installed) |
| Fonts | Self-hosted WOFF2 in `fonts/` (SIL OFL) |

**Two HTML entry points:** `index-dev.html` loads raw ES modules (no build needed — use for development); `index.html` loads the built `app.bundle.js` (production / Pages).

**Classic scripts loaded *before* the modules** (they set `window` globals the modules read): `data-embedded.js` (`window.__F1UNO_EMBEDDED`), `translations.js` (`window.__T` / `window.__BADGE_T`), `card-descriptions.js` (`window.__CARD_DESC`), and `cloud-config.js` (`window.__F1UNO_CLOUD`).

🇫🇷 JS vanilla en modules ES, point d'entrée `app.js`, bundle esbuild. Deux HTML : `index-dev.html` (modules bruts, dev) et `index.html` (bundle, prod). Quatre scripts classiques chargés avant les modules exposent des globals `window.__*`.

---

## 4. Run · Build · Test — exact commands

Run everything **from `Collections/F1/`**.

```bash
# ── DEV (no build): serve, then open index-dev.html ──
python3 -m http.server 8123
#   → http://localhost:8123/index-dev.html   (raw ES modules)

# ── BUILD the production bundle ──
npm install          # first time only — installs esbuild (the sole devDependency)
npm run build        # app.js → app.bundle.js (minified + sourcemap)
npm run dev          # same, unminified, rebuilds on change (watch)
#   → open http://localhost:8123/            (index.html, bundled)

# ── TEST (logic only, browser-free) ──
npm test             # node --test over tests/*.test.js
npm run test:watch   # re-run on change

# ── QUICK SYNTAX CHECK on a single file (used often here) ──
node --check render.js
```

> Port note: the VS Code launch config and this project's history use **8123**; the README examples use 8000. Either works — pick one and be consistent. `data/*.json`, season switching and module loading only work over HTTP.

🇫🇷 Tout se lance depuis `Collections/F1/`. Dev : `python3 -m http.server 8123` puis `index-dev.html`. Build : `npm run build`. Tests : `npm test`. Vérif rapide : `node --check <fichier>.js`.

---

## 5. Conventions — how things are done here

**Event handling — delegation, not inline handlers.** User actions are wired through a single delegated listener in `app.js` on `[data-action="…"]` (and `[data-digit]`, `[data-card]`, etc.). To add an interaction: put `data-action="myAction"` in the HTML and add a `case 'myAction':` to the `switch` in `initEvents()`. **If the action writes to the collection, add it to the `VIEWER_BLOCKED` set** so viewer (read-only) mode blocks it. Avoid inline `onclick=`.
🇫🇷 Interactions via `data-action` + délégation dans `app.js`. Toute action qui **écrit** doit être ajoutée à `VIEWER_BLOCKED` (mode lecture seule).

**Module layout.** One concern per module (see the file map). Each file opens with a boxed banner comment (`══` border) naming its responsibility — keep that style. Imports are explicit named ES imports.

**i18n.** See Hard Rule 5. Badge texts live in `window.__BADGE_T`; card descriptions in `card-descriptions.js`.

**Rarity has two independent scales — do not conflate them.** (1) Card rarity is a **7-level** scale `epic · legendary · mythic · ultra · cosmic · divine · eternal` (no `common`/`rare` anymore), derived from role/champion base rarity with foil bonuses (+1 simple foil, +2 dual, +3 wild/promo/nitro, **clamped at `divine`**); owning a **complete set** (≥1 copy of every variant) raises the effective rarity by +1, clamped at `eternal` — so `eternal` is ONLY reachable via a complete set (in practice: champions). `divine` renders as an animated iridescent gradient; `eternal` as a black-gold animated chip with a golden tile glow, light sweep and ✦ sparks. (2) The **type-pill** colour/star ladder is a *separate* scale (`typeBadgeStyles` / `typeBadgeRarity` in `metadata.json`) and must not be changed when you touch card rarity. Any animation must respect `prefers-reduced-motion`.
🇫🇷 Deux échelles distinctes : la rareté de carte (7 niveaux `epic…eternal` ; set complet ⇒ +1, seuls les foils clampent à `divine`) et l'échelle des pastilles de type (`typeBadgeStyles`). Ne pas les confondre. Animations : respecter `prefers-reduced-motion`.

**`localStorage` keys.** Season-scoped **v2** format: `f1uno_owned_<year>`, `f1uno_badges_<year>`, `f1uno_auto_badges_<year>`, `f1uno_history_<year>`. Shared keys: `f1uno_theme`, `f1uno_lang`, `f1uno_font`, `f1uno_title`, `f1uno_onboarded`, PIN/viewer keys, backup-reminder keys, `f1uno_install_dismissed`, and `f1uno_cloud_session`. Legacy **v1** keys migrate automatically on load — don't break `migration`.
🇫🇷 Clés `localStorage` scoppées par saison (v2). Migration v1→v2 automatique : ne pas la casser.

**Code style & language.** 2-space indent, semicolons, single quotes. Existing code comments and `log()` strings are a **French/English mix** — that is fine; keep comments clear in whichever language fits, but **all identifiers and user-facing i18n keys stay English**.

**Commit messages — Conventional Commits. HARD RULE for git.** Format: `type(scope): subject`, lowercase type and scope, imperative, an em-dash (`—`) for the elaboration. Types in use: `feat, fix, docs, style, refactor, test, chore, build`. Real examples from this repo:
- `feat(cloud): step 1 — cloud-config.js (Supabase URL + anon key slots)`
- `fix(fonts): apply font vars inline via JS so a stale stylesheet can't break switching`
- `test: first unit-test suite — 65 tests over the browser-free logic`
- `docs: sync README with current app (fonts, rarity, collector tools, PWA, tests)`

🇫🇷 Commits en Conventional Commits : `type(scope): sujet` en minuscules, à l'impératif, tiret cadratin `—` pour préciser. Types : `feat, fix, docs, style, refactor, test, chore, build`.

### DEUX SESSIONS SUR LE MÊME DÉPÔT — ce qui n'est PAS local

**Une opération git qu'on croit locale a un effet chez l'autre
session.** Le dépôt est partagé, l'arbre de travail aussi, l'index
aussi. Trois formes se sont produites le 23/08/2026, toutes dans la
même journée (voir n°43) :

1. **`git stash`** prend le travail en vol de l'autre session — fichiers
   modifiés ET non suivis — et le fait disparaître de son arbre ;
2. **`git push`** publie tous les commits ancêtres, donc ceux que
   l'autre session a faits localement et n'a pas décidé de publier ;
3. **une doc éditée des deux côtés** fusionne sans conflit et rend un
   fichier cohérent en apparence.

**RÈGLE 1 — TOUTE COMMANDE QUI TOUCHE L'INDEX, LE STASH, L'HISTORIQUE
OU LA PUBLICATION S'ÉPROUVE DANS UN CLONE JETABLE.** Jamais dans le
dépôt vivant :

    git clone --no-hardlinks . /tmp/bac && cd /tmp/bac

Ça vise `stash`, `reset --hard`, `checkout --`, `clean`, `rebase`,
`filter-branch`, l'essai d'un hook, et toute manipulation d'index. Le
clone coûte deux secondes ; l'incident coûte le travail de quelqu'un
d'autre, **et ne laisse aucune trace à chercher après coup**.

**RÈGLE 2 — AVANT DE POUSSER, REGARDER CE QU'ON POUSSE :**

    git log --oneline origin/main..HEAD

**S'arrêter si des commits qu'on n'a pas faits y figurent.** Ce n'est
pas une formalité : le 23/08, un push a emporté deux commits de l'autre
session, qui les croyait locaux et ne pouvait plus les amender.

**⚠️ CE GARDE NE PEUT PAS ÊTRE MÉCANISÉ PAR L'AUTEUR — mesuré.** Les
deux sessions commitent sous des identités RIGOUREUSEMENT identiques
(`Arts44 <amestdagt@proximus.be>` en auteur ET en committer), et leurs
horodatages sont entrelacés à la minute près : `0d7bcc4` à 09:25 d'une
session, `b390e7c` à 09:25 de l'autre. **Ni l'auteur ni l'heure ne
discriminent quoi que ce soit.** La seule chose qui le pourrait est ce
que la session ENREGISTRE elle-même — voir le chiffrage en n°43. En
attendant, la règle est une lecture humaine, et elle est écrite ici
pour qu'on sache qu'elle n'est pas automatisée.

### LIVRER — `scripts/livrer.sh`, jamais une chaîne de `&&` à la main

🇫🇷 **LE GARDE D'ÉTAT DE POSTE DE `livrer.sh` N'EST PAS UNE PARADE VERSIONNÉE.** Il refuse de livrer si le hook `pre-push`, `node_modules`, Playwright, son navigateur ou OpenCV manquent, avec la commande qui répare — mais il vit DANS le script, donc il ne protège que les livraisons qui passent par lui. Un `git push` direct depuis un poste neuf ne voit rien. C'est assumé et écrit sur place : le présenter autrement serait exactement le piège de ⑯.

**Une règle qui ne tient pas après trois occurrences n'est pas une
règle, c'est une intention.** Trois fois un push est parti avec un
filet rouge, alors que la règle du code de sortie était écrite ici. La
parade est désormais MÉCANIQUE, en deux couches :

```bash
scripts/livrer.sh          # build → tests → lint → les trois filets navigateur
LENTS=0 scripts/livrer.sh  # sans les filets (commit de documentation seule)
```

**`livrer.sh` et `git push` s'enchaînent avec `&&`, JAMAIS avec `;`.** Quatrième occurrence, et la première où la parade mécanique existait déjà : `livrer.sh ; git push` a poussé `adba070` sur un rouge, le point-virgule jetant le code de sortie que tout le reste du dispositif s'emploie à produire. Le hook n'a rien vu — il ne bloque que sur `npm test` et `npm run lint`, par dessein. **La parade existait, son opérateur l'a contournée par inattention** ; et le rouge était de surcroît un FAUX rouge (registre ⑲), ce qui rend ce genre de contournement d'autant plus facile à se pardonner sur le moment.

`set -euo pipefail` : au **premier** code non nul, tout s'arrête. Le
script monte et démonte lui-même le serveur local sur 8123 — trois fois,
un filet a rougi parce que le serveur d'une session précédente était
tombé, un rouge qui n'accusait pas le produit.

**Et un hook `pre-push`** (installé par `scripts/installer-hooks.sh`,
versionné parce que les hooks ne le sont pas) qui **refuse** le push si
`npm test` ou `npm run lint` échoue — vu rouge au contrôle. Il ne lance
QUE le rapide (~26 s) : un garde de plusieurs minutes se contourne avec
`--no-verify`, donc il vérifie seulement que les filets lents ont laissé
leur **trace horodatée** sur le commit poussé (`.git/derniere-livraison`),
et le dit sans bloquer sinon. **Rapide toujours, lent tracé.**

### MESSAGES DE COMMIT — pas de trailer Co-Authored-By

**Les messages de commit ne portent pas de trailer `Co-Authored-By`.**
L'auteur des commits est configuré en noreply (`git config user.email`,
local au dépôt) ; le trailer est un mécanisme **SÉPARÉ** que GitHub lit
pour créditer un second contributeur, et le retirer de la configuration
d'auteur ne le retire pas du message. **Les deux se traitent
indépendamment.**

L'historique antérieur **n'est pas réécrit** : cinq tags, cinq releases
et le lien Codacy en dépendent, pour un gain nul.

> ⚠️ **La source de ce trailer n'est ni le dépôt ni git** — ni gabarit
> (`commit.template`), ni hook, ni consigne écrite ici : c'est
> l'environnement d'exécution de l'assistant qui l'ajoute par défaut à
> chaque message. Cette entrée existe pour que la consigne du mainteneur
> l'emporte explicitement sur ce défaut, plutôt que d'être réappliquée à
> chaque commit et corrigée à la main.

### VÉRIFICATION CODACY APRÈS PUSH

**Après chaque push, lancer l'analyse Codacy et rapporter le résultat.** Ne pas se contenter du dashboard, qui analyse en différé : utiliser la **CLI locale**, qui donne le résultat immédiatement sur le code qui vient de partir.

```bash
codacy repository gh Arts44 f1-uno-elite --reanalyze
codacy issues -o json          # le compte, après
```

**Trois issues de conduite :**

- **Nouvelle issue introduite par le commit** → la corriger dans un commit de suivi, **sans attendre qu'on le demande**.
- **Faux positif** → le marquer avec sa justification écrite (`codacy issue … -I -R FalsePositive -m "…"`), **jamais désactiver le pattern**. Doctrine établie : un pattern éteint cache aussi les vrais cas ; une issue marquée documente une décision. Reporter la même justification dans `.codacy.yml`, qui doit rester lisible sans accès au service.
- **Issue préexistante sans rapport avec le commit** → la **signaler**, ne pas la traiter dans la foulée.

**Rapporter systématiquement le nombre d'issues AVANT et APRÈS.** Un push qui augmente le compte sans que ce soit dit est un push qui **dégrade en silence**.

> ⚠️ **Si l'authentification CLI a expiré, LE DIRE** — ne pas sauter l'étape en silence. Une convention qu'on saute sans le dire est pire qu'une convention absente : elle fait croire à une vérification qui n'a pas eu lieu. C'est exactement le motif de `check_seed_honesty()`, qui plantait **avant** son rapport et laissait supposer un contrôle passé. Formulation attendue : « analyse Codacy NON faite — authentification CLI expirée », suivie de `codacy login`.

> **Deux pièges déjà payés.** ① Un marquage est attaché à un **emplacement**, pas à une décision : `SESSION_KEY` a rouvert en passant de `cloud.js:25` à `cloud-auth.js:34`. Après tout découpage qui déplace une ligne marquée, revérifier. ② Les patterns imposés par le **coding standard de l'organisation** ne peuvent être ni décochés ni désactivés au niveau du dépôt (`Pattern enforced by Default coding standard, can't be modified`) — le marquage occurrence par occurrence est alors le seul levier, et c'est une dette à écrire, pas un choix.

🇫🇷 Après chaque push : relancer l'analyse via la **CLI** (pas le dashboard), rapporter le compte **avant/après**. Nouvelle issue du commit → commit de suivi immédiat. Faux positif → marquer avec justification, jamais éteindre le pattern. Issue préexistante → signaler seulement. **Auth expirée → le dire, ne jamais sauter en silence.**

---

## 6. File map / Carte des fichiers

```
Collections/F1/                 ← git root · all commands run here
├── index.html                  # Production markup → loads app.bundle.js
├── app/index-dev.html              # Dev markup → loads app.js as native ES modules (no build)
├── app/styles.css                  # Themes, grid, cards, responsive
├── manifest.webmanifest        # PWA manifest (relative start_url/scope)
├── sw.js                       # Service worker — SW_VERSION, precache, cache-first  ⚠ classic script, not bundled
├── favicon.ico · icons/ · screenshots/ · fonts/   # PWA assets (self-hosted fonts)
│
│  ── Bundled ES modules (entry app.js → app.bundle.js) ──
├── app/app.js                      # Entry: init, global event delegation, SW registration
├── app/logger.js                   # DEBUG-gated log()/warn()
├── app/i18n.js                     # LANGS, t(), applyLanguage()
├── app/data.js                     # DB constants, JSON loaders, season switching
├── app/storage.js                  # localStorage, collection state, import/export, migration
├── app/backup.js                   # Backup code + QR (compress/encode, #backup= link, reminder)
├── app/qrcodegen.js                # Vendored QR encoder (Nayuki, MIT)
├── app/history.js                  # Daily owned-count snapshots (stats progression)
├── app/collector.js                # Missing / doubles / trade-list logic (pure, unit-tested)
├── app/tutorial.js                 # Interactive sandboxed guided tour
├── app/install.js                  # PWA install (native prompt, banner, per-platform + Arc)
├── app/badges.js                   # Badge evaluation/rendering + titles
├── app/stats.js                    # computeStats(), renderStats() (progression, highlights, donut)
├── app/render.js                   # Grid, modal, views, toast
├── app/pagehead.js                 # Bandeau d'en-tête commun aux 5 pages (icône, titre, sous-titre, actions, mesure)
├── app/pin.js                      # Auth/PIN, viewer & admin modes, settings UI
├── cloud.js                    # Optional Supabase push/pull via raw REST fetch (magic-link auth)
├── app/settings-sync.js            # Optional settings in backups (prefs / security categories)
├── app/update.js                   # Auto-updates: SW registration, reload banner, what's-new offer
├── app/changelog.js                # Version history ×7 languages — APP_VERSION = newest entry
├── app/secure-store.js             # Optional at-rest encryption (PBKDF2+AES-GCM, PIN-keyed) of collection data
├── app/feedback.js                 # In-app feedback → Supabase (REST pur, RLS own-rows, connexion cloud requise)
│
│  ── Classic scripts (NOT bundled · loaded before modules · set window.__ globals) ──
├── app/translations.js             # window.__T / window.__BADGE_T (7 languages)
├── app/card-descriptions.js        # window.__CARD_DESC
├── app/data-embedded.js            # window.__F1UNO_EMBEDDED (offline fallback data)
├── app/cloud-config.js             # window.__F1UNO_CLOUD (Supabase url + anon key)
│
├── app/app.bundle.js(.map)         # ⚠ GENERATED + COMMITTED — rebuild after editing a bundled module
├── package.json                # esbuild build/dev + node --test scripts (esbuild = only devDep)
├── extract_data.mjs            # Dev tool: data extraction/generation
├── extract_circuits.mjs        # Dev tool: circuit paths from GPS traces
├── capture_screenshots.py      # Dev tool: regenerates EVERY screenshot (Playwright).
│                               #   Deterministic seed computed from data/cards-2025.json;
│                               #   needs `python3 -m http.server 8124` running. Exits 1 if
│                               #   the Eternal-tile sparkles overlap anything.
├── tests/                      # node --test suites + fixtures (_setup.js, _fixtures.js)
├── docs/CLOUD-SYNC-DESIGN.md   # Cloud-sync design document
└── data/
    ├── metadata.json           # cardTypes, rarities/rarityKeys/rarityOrder, typeBadgeStyles, teams, drivers…
    ├── cards-2025.json         # 101 cards (2025 season)
    ├── circuits.json           # SVG circuit outlines
    └── badges.json             # 120 badges (68 auto + 52 manual)
```

**Adding a season:** create `data/cards-<year>.json` (same shape as `app/data/cards-2025.json`), update `app/data/metadata.json` for new teams/drivers, and remember the header only lists 2025 + seasons already present in `localStorage`.

### Where to start, by task type / Par où commencer selon la tâche

| Task / Tâche | Read first / Lire d'abord | Then / Ensuite |
|---|---|---|
| UI bug (grid, modal, toast, views) | `app/render.js`, `app/styles.css` | `data-action` wiring in `app/app.js` |
| Logic bug (stats, badges, collection) | `app/stats.js` / `app/badges.js` / `app/collector.js` / `app/storage.js` | matching suite in `tests/` |
| i18n (new text, translation fix) | `app/translations.js` (×7 langs), `app/i18n.js` | badges: `__BADGE_T` · cards: `app/card-descriptions.js` |
| Card data / seasons | `app/data/cards-2025.json`, `app/data/metadata.json`, `app/data.js` | keep `app/data-embedded.js` in parity |
| PWA / offline / update | `app/sw.js` (`SW_VERSION`), `app/update.js`, `app/install.js` | `manifest.webmanifest` |
| Cloud sync | `app/cloud-auth.js` · `app/cloud-http.js` · `app/cloud-sync.js` · `app/cloud-ui.js`, `app/cloud-config.js` | `docs/CLOUD-SYNC-DESIGN.md`, `tests/cloud.test.js` |
| PIN / viewer / security | `app/pin.js`, `app/secure-store.js` | `VIEWER_BLOCKED` set in `app/app.js` |
| Release / version | `app/changelog.js` (`APP_VERSION`), `app/sw.js` | Definition of Done (§8) |

**Never read / Ne jamais lire :** `node_modules/`, `app.bundle.js(.map)` (generated — rebuild, never hand-edit), `package-lock.json`, `.git/`. **Assets only / Assets :** `icons/`, `screenshots/`, `fonts/`, `favicon.ico`.

🇫🇷 Tableau d'orientation : pour chaque type de tâche, les fichiers à lire en premier. Ne jamais lire les dossiers générés ou les assets binaires.

---

## 7. Cloud sync — current status (read carefully)

⚠️ **The README's "Roadmap" section is out of date on this point.** It still says cloud sync has *"no code written."* That is **no longer true**: an **optional Supabase cloud layer is now in the repo and wired up**:
- `cloud.js` — magic-link auth + session handling over raw REST `fetch()` (no SDK).
- `cloud-config.js` — filled with a real project `url` + `anon` key (public by design).
- `cloud.test.js` — unit tests for the pure helpers.
- `app.js` calls `handleAuthRedirect()` at boot; `pin.js` renders the cloud Settings section.

It is **optional and gated**: with `cloud-config.js` empty the app behaves exactly as before. When working on cloud features, keep the raw-`fetch()`/no-SDK rule (Hard Rule 1), the anon-key-only rule (Hard Rule 7), and ensure `app/sw.js` continues to **exclude the Supabase origin** from caching (cloud responses use `cache:'no-store'` and must never be served from cache). If you touch this area, also update the README roadmap so docs match reality.

🇫🇷 Le README dit à tort que la sync cloud n'existe pas : elle est désormais présente (`cloud.js`, `cloud-config.js`, `cloud.test.js`, appelée dans `app.js`). Optionnelle, activée seulement si `cloud-config.js` est rempli. Respecter : `fetch` brut (pas de SDK), clé `anon` seulement, et le SW qui **exclut** l'origine Supabase du cache.

---

## 8. Definition of Done / Définition de « terminé »

A change is **not done** until all of these pass. Run them before you say the work is finished.

1. **Syntax:** `node --check <each edited .js>` is clean.
2. **Tests:** `npm test` is green. If you changed testable logic (rarity, storage/migration, backup, stats, badges, history, collector, install detection, language gating, cloud helpers), **add or update a test** in `tests/`.
3. **Bundle:** if you edited a *bundled* module, `npm run build` was run and `app.bundle.js` + `.map` are committed alongside the source.
4. **Service worker:** if any precached shell file changed, `SW_VERSION` in `app/sw.js` was bumped and the precache list still covers every shipped file.
5. **i18n:** any new user-facing string exists as a key in **all 7 languages** in `translations.js`; nothing is hard-coded.
6. **Relative paths:** no new URL starts with `/`.
7. **Logging:** no stray debug `console.log`; `DEBUG` is `false`.
8. **Manual browser pass (what tests can't cover) — et ce n'est PAS une formalité de fin de chantier.**

   > ⚠️ **Cas vécu, 1.53.0.** L'app affichait le catalogue de la saison précédente sous l'étiquette d'une saison sans données. Un état vide a été câblé dans `renderGrid`, les **tests unitaires étaient tous verts** — et la vérification navigateur a montré que le correctif ne corrigeait rien : 10 cartes de 2026 s'affichaient sous « 2027 », avec un bandeau annonçant « 10 des 20 cartes de 2027 ». Le silence avait été remplacé par une **fausse information**, ce qui est pire. La vraie cause était deux couches plus haut, dans `loadAppData()`.
   >
   > Les tests unitaires ne voyaient rien parce qu'ils testaient la fonction corrigée, pas le chemin réel. **Un test vert ne prouve pas qu'un défaut est corrigé — il prouve que ce qu'on a pensé à tester passe.**

    serve over HTTP and check what you touched — DOM rendering, modal, theming, font switching, animations (incl. `prefers-reduced-motion`), the tutorial's DOM flow, PWA install, offline reload, and QR visual output. State clearly which manual checks you did and which you couldn't.
9. **Docs:** if behaviour or structure changed, update `README.md` (and this file) so they still match reality.

> **Hors du dépôt, donc hors de portée des tests : la DESCRIPTION GitHub.**
> Le champ « About » du dépôt (visible en haut de la page, et dans les
> résultats de recherche) contient des chiffres qu'aucun test ne peut
> atteindre — il a annoncé « 416 tests » longtemps après que le compte
> réel ait changé. À vérifier à la main à chaque fois qu'un chiffre du
> README bouge. Même remarque pour les *topics* et le lien de site.
>
> 🇫🇷 La description GitHub n'est pas dans le dépôt : aucun test ne la
> couvre. La relire quand un chiffre change.

🇫🇷 Avant de dire « terminé » : `node --check` propre, `npm test` vert (+ test ajouté si logique modifiée), bundle reconstruit et commité si module bundlé, `SW_VERSION` incrémenté si le shell change, 7 langues à jour, aucun chemin en `/`, pas de `console.log` de debug, passe manuelle navigateur (HTTP) sur ce que tu as touché, et docs synchronisées.

---

## 9. Gotchas — things that used to live only in the maintainer's head

- **The pre-commit hook is not in git.** After a fresh clone, nothing auto-rebuilds the bundle. Rebuild manually (or recreate the hook) or you'll commit a stale `app.bundle.js`.
- **Editing `translations.js` / `data-embedded.js` / `card-descriptions.js` / `cloud-config.js` / `app/sw.js` does NOT need a rebuild** — they're classic scripts outside the bundle. Editing any other root `*.js` **does**.
- **Stats progression history has no back-fill** — the curve starts empty the day the feature was installed. Don't try to reconstruct past data; there's no per-card timestamp.
- **`data-embedded.js` must stay in parity with the real `data/metadata.json`** (a few tests assert this) — it's the offline fallback.
- **Viewer mode** silently blocks write actions via the `VIEWER_BLOCKED` set in `app.js`; forget to list a new write action there and read-only mode leaks writes.
- **UN MEILLEUR RÉGLAGE DE QUALITÉ N'EST PAS UN GAIN : IL DÉPLACE LE CURSEUR SUR LA MÊME COURBE. CE QUI ROUVRE UN DOSSIER, C'EST CE QUI DÉPLACE LA COURBE.** La règle est née d'un chantier d'images — un encodeur candidat rendait 17 % d'octets en moins, et il les rendait en échange de fidélité, exactement comme l'aurait fait l'encodeur en place réglé plus bas. Mais elle ne vaut pas que pour les images : **c'est le motif de la moitié des faux chantiers de performance.** Baisser une qualité, réduire un nombre d'échantillons, raccourcir une durée, élargir une tolérance, sauter une vérification — tout cela « gagne » quelque chose, et tout cela se paie sur le même axe. Avant d'ouvrir un chantier de performance, poser la question : **est-ce que je propose un point différent sur la courbe existante, ou une courbe différente ?** Si c'est le premier cas, ce n'est pas un chantier, c'est un arbitrage — et il doit se présenter comme tel, avec les deux termes de l'échange chiffrés. Corollaire pour les mesures comparatives : deux candidats ne se comparent qu'à **qualité constante** ou à **octets constants**, jamais en laissant les deux bouger.
- **UN DÉFAUT PEUT VIVRE DANS LA TRANSITION, LÀ OÙ AUCUNE MESURE D'ÉTAT NE LE VOIT.** Famille distincte des instruments menteurs : ici l'instrument dit vrai, il regarde simplement au mauvais MOMENT. Vécu au signalement « la barre flottante masque les cartes » (1.72.0) : la mesure d'état — position du dernier bloc contre le haut de la barre, aux quatre tailles d'écran, iPad compris — répondait « rien n'est recouvert », et c'était exact **en fin de défilement**. Le défaut était PENDANT le défilement : la barre opaque tranchait net les lignes qui passaient dessous. Aucune mesure de repos ne pouvait l'attraper ; il a fallu **regarder une capture prise à mi-course**. **Règle : pour tout ce qui bouge — défilement, ouverture, transition, animation — mesurer AUSSI un instant intermédiaire, ou juger sur l'image plutôt que sur les nombres.** Corollaire : un utilisateur qui décrit un défaut « en enregistrant image par image » décrit très souvent un défaut de transition — le prendre au mot avant de conclure qu'il n'est pas reproductible.
- **« LA SUITE LOGIQUE » N'EST PAS UN ARGUMENT.** Une fonctionnalité se justifie par le besoin qu'elle sert, mesuré ou observé — jamais par la symétrie, la complétude ou l'élégance de ce qui existe déjà. **La question est toujours la même : quelle fonctionnalité ça débloque, et pour quel besoin ATTESTÉ ?** Quatre refus l'ont opposée, sur quatre sujets sans rapport : ① le découpage de `storage.js` (aucune fonctionnalité débloquée — le module était gros, pas bloquant) ; ② celui de `tutorial.js` (même mesure, même verdict) ; ③ une huitième langue (aucun utilisateur ne l'a demandée ; sept sont déjà à maintenir) ; ④ la fiche d'accord partageable du recoupement (le besoin observé — trois personnes s'échangeant des listes dans un fil — s'arrête à « voici l'échange » ; le protocole de réponse est une invention). **Corollaire : un refus se consigne AVEC son argument et sa condition de réouverture**, sinon il se rejoue dans six mois sous le nom de « la suite logique » — c'est-à-dire sans argument du tout. Les quatre sont dans `docs/POINTS-SIGNALES.md` et `docs/V2-REFACTOR.md`, chacun avec ce qui le rouvrirait : un retour d'usage réel, jamais une intuition de symétrie.
- **UN CONTEXTE « VIERGE » AU SENS DES DONNÉES N'EST PAS UN PREMIER LANCEMENT — et un élément PRÉSENT n'est pas un élément ATTEIGNABLE.** Deux moitiés d'une même règle, chacune payée séparément. ① Un `localStorage` vidé saute le choix de langue, la mise en route et la proposition de tutoriel : **toute fonctionnalité atteignable par un lien externe se vérifie depuis un profil qui n'a JAMAIS vu l'app**, ces trois écrans compris (`seed_jamais_lancee()` + `franchir_premier_lancement()` dans `capture_seed.py`, qui RENVOIE les étapes franchies pour qu'un test ne puisse pas passer au vert sans les avoir rencontrées). ② **Ce qui apparaît pendant la mise en route se vérifie en ATTEIGNABILITÉ, jamais en présence DOM** : `document.getElementById()` rend `true` pour un élément couvert par un écran de priorité supérieure — mesurer avec `elementFromPoint` sur la cible réelle. **TROIS fois payé** : les six défauts de l'état « 1 carte » (jamais rendu par une capture), la fiche d'échange perdue derrière la visite guidée, et le bandeau de mise à jour **présent 6 fois sur 6, atteignable 0 fois sur 6** derrière l'écran de langue — ce dernier ayant d'abord donné un faux « 10/10 » à un banc qui interrogeait le DOM. La règle du moment vit dans `app/moment.js`, partagée : une surcouche ENREGISTRE toujours, et ne diffère que l'affichage.
- **SOUPÇONNER L'INSTRUMENT AVANT LE PRODUIT.** C'est UNE règle, pas une collection d'anecdotes : quand une mesure accuse le code, elle énonce une hypothèse, pas un verdict. Avant de corriger quoi que ce soit, faire rendre à l'instrument le cas RÉEL et vérifier qu'il sait le mesurer. **Ordre de suspicion : l'instrument, puis la chaîne (bundle reconstruit ? cache purgé ? bon fichier ?), puis le contrôle, et le produit en DERNIER.** La règle a de nombreuses façons connues de se manifester — **le registre ci-dessous n'annonce délibérément aucun total** : un compte écrit ici cesserait d'être vrai au premier ajout sans que rien ne le signale, ce qui est exactement ⑦ — les reconnaître fait gagner l'exécution qui les démasque, mais c'est la règle qu'on retient, pas la liste :
  - ① **la condition de rendu n'est pas celle du terrain** (un banc de lisibilité à densité 3 « prouvait » qu'aucun trait fin ne survit à 14 px — faux à densité 1, la densité réelle)
  - ② **l'API répond à côté de la question** (`getBBox()` sur un `<use>` rend 0…120 sur un tracé qui n'occupe pas son cadre — mesurer au raster)
  - ③ **la donnée de test est dégénérée** (un motif répétitif, `'A'`×700, trompe un détecteur qui marche très bien sur une charge réelle)
  - ④ **on mesure un état de repos alors que le défaut vit dans la TRANSITION** (« rien n'est recouvert » était exact en fin de défilement et faux pendant — voir l'entrée dédiée ci-dessous)
  - ⑤ **on lit la sortie d'une commande sans lire son code de sortie** (un `grep` ou un `| tail` renvoie 0 et laisse passer un échec — voir l'entrée dédiée). **CE CAS-CI SE REPRODUIT PLUS QUE LES AUTRES, ET PAS POUR LA MÊME RAISON** : ce n'est pas un défaut de raisonnement, c'est un réflexe de frappe — `commande | tail -3 ; echo $?` s'écrit tout seul, et la parade demande de penser à quelque chose au moment précis où la main tape vite. Une parade ne corrige pas un réflexe. **PISTE, NON FAITE : en faire un outil plutôt qu'une règle** — un alias ou une petite commande du dépôt qui exécute, capture la sortie dans un fichier ET rende le vrai code, de sorte que la forme naturelle soit aussi la forme juste. À chiffrer le jour où ce cas se reproduira encore ; c'est la même logique que ⑰, où une parade qui échoue plusieurs fois appelle un mécanisme et non une meilleure formulation.
  - ⑥ **le harnais de rendu n'a été exécuté que dans UN ordre d'insertion** (deux surcouches à `z-index` égal : le test d'atteignabilité interroge celle qui est arrivée en dernier, conclut « atteignable », et ne voit jamais que l'autre est intégralement recouverte — POINTS-SIGNALES n°24). **Parade : tout test de coexistence de surfaces s'exécute dans LES DEUX ORDRES**, et mesure l'atteignabilité des DEUX surfaces, pas seulement de celle qu'on soupçonne.
  - ⑦ **un harnais refait rend des chiffres qu'on ne réconcilie pas avec la campagne précédente** (la même paire mesurée à 47 px puis à 30 px : aucun des deux relevés n'était faux — le premier portait un contenu de toast synthétisé plus long que le contenu réel, et le changement d'instrument n'avait pas été déclaré. Un écart non expliqué se lit comme une correction silencieuse d'un chiffre publié). **Parade : toute mesure qui en remplace une autre cite l'ancienne, donne le nouveau chiffre, et EXPLIQUE l'écart — un chiffre qui bouge sans raison écrite invalide les deux.** **Et la parade vaut À L'INTÉRIEUR d'un même rapport, pas seulement d'une campagne à l'autre** : un écart de coût annoncé entre deux options se recalcule poste par poste avant d'être écrit — un chiffre asséné qui porte une recommandation est pire qu'un chiffre absent. **TROIS RÉCIDIVES APRÈS L'ÉCRITURE DE CETTE PARADE, ET C'EST LA DONNÉE QUI COMPTE :** un rapport annonçant « 9 lignes » sur un périmètre, un livrable comparant −12 à +62 sur deux jeux de postes, et la comparaison de `styles.css` elle-même — deux jeux de postes, encore. **Les trois ont été relevées par le mainteneur, jamais par l'auteur du chiffre.** Une parade que son auteur n'applique jamais n'est pas une parade : c'est une intention. D'où le passage de l'intention au MÉCANISME, et il ne se déclenche pas à la relecture mais **AU MOMENT DE RENDRE LE CHIFFRE** — poste de vérification obligatoire, au même titre que le contrôle négatif sur un garde : **tout chiffre comparé à un chiffre antérieur DÉCLARE EXPLICITEMENT SON PÉRIMÈTRE, et les deux périmètres sont MONTRÉS CÔTE À CÔTE.** Pas « j'ai vérifié que c'est comparable » — les deux périmètres écrits, l'un sous l'autre, dans le rapport que le lecteur a sous les yeux. Un chiffre dont le périmètre n'est pas écrit à côté de celui auquel on le compare ne se rend pas : il se recalcule ou il se tait.
  - ⑧ **on rapporte une ABSENCE sans avoir prouvé que la chose avait le droit d'apparaître** (« badgeToast : ABSENTE » n'était pas un fait du produit mais un ordre d'appel du harnais — `_drainToastQueue()` diffère de 1200 ms quand le toast standard est déjà là). **Parade : une absence n'est un résultat qu'accompagnée d'un CONTRÔLE POSITIF — la même surface obtenue dans les conditions où elle doit paraître.** C'est le pendant exact du contrôle négatif exigé sur les gardes. **TROISIÈME RÉCIDIVE, 21/08/2026, et une variante qui mérite sa propre parade :** affirmation que « 44 n'apparaît nulle part » pour Lewis Hamilton, tirée d'une lecture du champ `number` des cartes — qui vaut 31, 32, 33 et porte le rang dans le set, pas le numéro de course. **Le 44 existait, ailleurs sur le chemin de rendu** : `metadata.json → driverNumbers`, indexé par NOM de pilote, lu par `DRIVER_NUMBERS[card.name]?.n`. L'utilisateur avait sous les yeux quatre contre-exemples à l'écran (#001 Albon 23, #004 Alonso 14, #007 Antonelli 12, #010 Bearman 87) qui suffisaient à réfuter l'affirmation, et je ne les avais pas cherchés. **Parade spécifique : pour une absence dans l'AFFICHAGE, c'est le CHEMIN DE RENDU qu'on suit, pas le champ de données qui semble porter le nom attendu.** Partir de ce qui est peint, remonter au code qui le peint, puis seulement aux données — l'inverse conclut sur le premier champ dont le nom ressemble. **QUATRIÈME RÉCIDIVE, 21-22/08/2026, ET LA FORME LA PLUS COÛTEUSE DE TOUTES : une absence conclue SIX FOIS de suite, puis inscrite en documentation, où elle a produit un mécanisme.** Il a été affirmé sur six tours que « l'outil Codacy n'est pas disponible dans la session ». **Il analysait chaque jour, sans interruption, y compris les deux jours déclarés injoignables.** Trois erreurs superposées, toutes vérifiées dans les traces : un `which codacy` rendant « not found » dans un shell alors que deux autres invocations de la MÊME session trouvaient `/opt/homebrew/bin/codacy` (le PATH n'est pas le même d'une invocation à l'autre) ; un `ls -la ~/.codacy/ 2>/dev/null && … || echo absent` rendu non nul pour une raison qui n'était pas l'inexistence — le répertoire existait et contenait `credentials` ; et `CODACY_API_TOKEN` absent de l'environnement, vrai mais sans conséquence, la CLI lisant `~/.codacy/credentials` et **l'API HTTP publique du dépôt ne demandant aucun jeton**. **CE QUE CES TROIS ONT EN COMMUN EST LE DÉFAUT : aucune n'est un test de l'OUTIL. Ce sont trois tests de PRÉCONDITIONS — le binaire est-il là, le jeton est-il là, le répertoire est-il là — dont l'échec a été converti en absence sans qu'un seul APPEL soit tenté.** Un `curl` de vingt caractères aurait réussi n'importe lequel des six tours, sans binaire et sans jeton. **CE QUI REND CE CAS PIRE QUE LES TROIS PRÉCÉDENTS : l'absence n'est pas restée dans une réponse, elle est entrée dans POINTS-SIGNALES, où elle est devenue la PRÉMISSE d'un mécanisme — une borne, une règle de recalcul, et une deuxième mesure locale prise en repli d'une panne qui n'existait pas.** Une erreur consignée se reproduit sans être re-commise : elle est lue. **Parade renforcée : une absence ne s'écrit dans un document qu'accompagnée de L'APPEL QUI ÉCHOUE, recopié.** Et quand plusieurs chemins mènent au même service — CLI locale, API HTTP —, l'échec de l'un ne dit rien de l'autre. **PART DU DEMANDEUR, notée parce qu'elle a reconduit l'erreur cinq fois :** la consigne répétée était « si l'outil est encore absent, dis-le » — elle demandait une DÉCLARATION, pas une vérification, et acceptait donc l'absence comme un état à rapporter au lieu d'exiger qu'on l'éprouve. **La formulation qui casse la boucle est « montre l'appel qui échoue ».** Elle vaut pour toute demande d'état d'un outil externe.
  - ⑨ **on simule le geste au lieu de le faire** (`element.click()` déclenche le gestionnaire QUELLE QUE SOIT la valeur de `pointer-events` : il n'y a aucun test de survol. Un filet qui vérifiait qu'aucun clic ne passe sous un voile en comptait six, avec un CSS parfaitement correct — l'instrument accusait le produit). **Parade : un geste se joue au niveau du DISPOSITIF — la souris à des coordonnées, le clavier par des touches — jamais par l'API qui court-circuite la couche qu'on prétend tester.** Bonne nouvelle enfouie dans ce cas : c'est le CONTRÔLE NÉGATIF qui l'a démasqué, en refusant de rougir là où il devait. Un contrôle négatif ne vérifie pas seulement le garde, il vérifie l'instrument.
  - ⑩ **on mesure la BOÎTE et pas le CONTENU** (une surface peut faire 351 px de large pendant que son bouton est à x=470 sur un écran de 375 : un conteneur ne rétrécit pas ce que `min-width:auto` l'empêche de rétrécir. Le filet rendait vert sur des boutons hors écran). **Parade : tout contrôle de géométrie descend jusqu'aux DESCENDANTS — bornes de l'écran, chevauchements, atteignabilité — parce qu'un parent conforme ne prouve rien de ses enfants.**
  - ⑪ **la donnée de test est un GABARIT MAL SUBSTITUÉ** (le contenu synthétique d'un badge-toast, dont les expressions imbriquées n'avaient pas été remplacées, produisait un libellé plus court que le vrai — donc une surface plus BASSE que la vraie, donc une scène plus facile que la réalité). **C'est une RÉCIDIVE de ⑧, et il faut dire pourquoi la parade ne l'a pas empêchée : ⑧ était écrite pour les ABSENCES (« prouver que la chose avait le droit de paraître »), et ici la chose était bien là — seulement fausse. Une parade formulée sur le symptôme qu'on venait de vivre ne couvre pas la variante d'à côté.** Parade élargie, et c'est la formulation qui compte : **présent ne veut pas dire conforme. Le contrôle positif porte sur le CONTENU RÉEL — les vraies chaînes, dans les vraies langues — et un instrument neuf hérite des parades des instruments précédents avant sa première exécution, pas après son premier mensonge.**
  - ⑫ **on prend « absent de mon contexte » pour « inexistant »** — c'est ⑧ appliqué à sa propre mémoire, et c'est le cas le plus facile à commettre parce qu'il ne ressemble pas à une mesure. Vécu à la fin du chantier de la zone basse : affirmation qu'une section n'avait jamais été écrite, alors qu'elle l'avait été et que l'utilisateur en détenait le texte — la conversation avait simplement été résumée entre-temps. Une absence conclue **sans contrôle positif**, exactement comme ⑧, à ceci près que la chose manquante était un souvenir. **Parade : avant d'affirmer qu'une chose n'a pas été écrite, la CHERCHER dans l'historique — transcription, journal git, fichiers de session. Et à défaut de preuve, dire « je ne le retrouve pas » et non « ça n'existe pas » : les deux phrases n'engagent pas la même chose. LE FIL DE L'UTILISATEUR FAIT FOI SUR MA RECONSTRUCTION**, toujours, y compris quand ma reconstruction est confiante — la confiance est ici l'exacte mesure du danger, puisqu'un contexte résumé ne signale pas ce qu'il a perdu.
  - ⑬ **on SAUTE un contrôle d'instrument parce qu'il a déjà été passé une fois.** La justification a l'air raisonnable — « ce fait est consigné, le refaire coûterait une exécution pour rien » — et elle est fausse : un contrôle d'instrument ne mesure pas le monde, il mesure L'INSTRUMENT, et l'instrument d'aujourd'hui n'est pas celui d'hier. Version du navigateur, du pilote, du serveur de test, une dépendance qui a bougé : le fait consigné peut rester vrai pendant que le harnais, lui, a changé d'état. **C'est ⑦ appliqué aux instruments et non aux chiffres : on ne réutilise pas une calibration sans la refaire, comme on ne réutilise pas un chiffre sans redéclarer son périmètre.** Parade : **un contrôle d'instrument se rejoue à chaque campagne, même quand on connaît sa réponse — et surtout quand on la connaît**, puisque c'est le seul cas où l'on serait tenté de ne pas regarder le résultat. Un contrôle dont on prédit l'issue sans l'exécuter n'a pas été fait.
  - ⑭ **la parade a été appliquée au SUJET observé, pas à l'OBSERVATEUR.** Cas vécu : un bras de mesure rapporté « 0 réussite sur 5 » alors que la fenêtre d'observation était plafonnée à **150 s** pour un chemin qui demande **~352 s**. Le plafond avait été choisi à trois fois la durée d'une installation saine (41,8 s) — une marge qui semblait large et qui ne l'était pas. **Le résultat n'était pas « jamais », il était « pas encore ».** C'est ⑧ au deuxième degré : la parade de ⑧ — prouver que la chose avait le droit d'apparaître — avait bien été appliquée au produit, et pas une seconde à ma propre fenêtre. **Une parade tournée vers le sujet ne protège pas celui qui mesure.** Parade : **toute fenêtre d'observation DÉCLARE SA VALEUR À CÔTÉ DU RÉSULTAT**, et un résultat négatif s'énonce « rien observé en N secondes », jamais « jamais ». La formulation n'est pas de la prudence d'écriture : « jamais » ferme l'enquête, « rien en N s » désigne le prochain essai. **PREMIER CAS RÉEL, le jour même où cette entrée a été écrite :** pendant la campagne du n°23, le binaire de Playwright a disparu du cache de la machine ENTRE DEUX MESURES — une campagne verte à l'exécution N, un « Executable doesn't exist » à l'exécution N+1, sans que rien du dépôt n'ait bougé. L'instrument avait bel et bien changé d'état en cours de route ; c'est exactement ce que la parade prétend attraper, et il n'a pas fallu attendre une deuxième campagne pour le voir. **Le coût d'un contrôle d'instrument est une exécution ; le coût de l'omettre est une correction inventée sur un défaut qui n'existe pas — ou, pire, un vrai défaut masqué par un banc trop indulgent.** **MÊME RÈGLE, APPLIQUÉE À UNE CAMPAGNE RÉTROSPECTIVE, 23/08/2026 :** un contrôleur a rejoué 172 états historiques de trois fichiers pour répondre à « l'incohérence de doc s'est-elle déjà produite en silence ? » et a rendu deux cas, aucun de concurrence. **Le zéro portait sur la mauvaise période.** Les invariants qui l'auraient détectée — les marqueurs d'état et l'en-tête chiffré de POINTS-SIGNALES — n'existaient que dans 3 états sur 58 : la classe qui avait cassé la veille était invérifiable sur 95 % de l'historique. Et la fenêtre où le phénomène EXISTE (deux sessions simultanées) ne compte que 6 commits sur ce fichier. **Une campagne rétrospective ne vaut que sur l'intersection de deux fenêtres : celle où le phénomène pouvait se produire, et celle où l'invariant qui le détecte existait déjà.** Ici l'intersection était quasi vide, et le chiffre « 0 sur 172 » le cachait au lieu de le dire. **DEUXIÈME LEÇON, plus dure : le défaut mesuré s'est produit PENDANT sa propre mesure.** Deux des quatre fautes injectées pour éprouver le contrôleur n'en étaient pas — les cases ㉚ et ㉛ que je croyais libres venaient d'être ajoutées par l'autre session. **Quand on mesure un phénomène encore actif, l'instrument mesure une cible qui bouge, et il faut le déclarer plutôt que de reprendre son chiffre.** Voir n°43.
  - ⑮ **la cause est DÉDUITE de la bonne famille de métriques, et elle est fausse D'AXE.** Vécu sur le décalage de mise en page de la vitrine : CLS mobile à 0,1929, tout le contenu poussé vers le bas au chargement de la police. Diagnostic annoncé — « le lockup grandit d'environ 23 px » — tiré des métriques VERTICALES de la police (ascendante 0,984 contre 0,967 pour le repli). Cohérent, plausible, et faux : mesuré en bloquant le téléchargement puis en le laissant venir, **le lockup ne bouge pas d'un pixel** (49,6 px avant et après). C'est le paragraphe qui gagne **+24,8 px, exactement une ligne** — la police est plus LARGE, le texte passe de quatre lignes à cinq. **Le décalage était horizontal d'origine**, et les deux descripteurs verticaux prescrits (`ascent-override`, `descent-override`) n'y pouvaient rien : c'est `size-adjust` qui corrige, 0,1929 → 0,0033. **Le remède avait été validé et posé comme non négociable avant que la cause soit mesurée — des deux côtés de la conversation — et c'est la mesure qui a rattrapé, pas la relecture.** Parade : **une cause n'est établie que par une mesure DIFFÉRENTIELLE — retirer l'entrée soupçonnée et regarder ce qui change**. Ici : bloquer la police et comparer les boîtes. Deux minutes, et elles auraient été prises avant d'écrire la prescription. Corollaire : quand une famille de métriques a deux axes, dire lequel on a mesuré ; « les métriques de police expliquent le décalage » ne désigne pas un axe.
  - ⑯ **le harnais de livraison rend vert sur un ÉTAT DE POSTE qui n'est vrai que chez son auteur.** Vécu sur `scripts/livrer.sh` : il montait un serveur sur le port 8123, alors que **trois filets sur cinq interrogeaient le 8124**. Ils passaient depuis des semaines grâce à un serveur lancé à la main qui traînait sur la machine ; et `verify_zone.py`, écrit lors de son propre chantier, n'avait **jamais été ajouté au script**. **Sur un clone propre, la livraison aurait échoué — ou pire, aurait sauté des filets sans le dire.** Le harnais censé empêcher les pushs rouges était lui-même adossé à une hypothèse invérifiée. Mesuré en poussant plus loin : si un port est déjà pris, `python3 -m http.server` meurt aussitôt, `set -e` ne le voit pas — c'est une tâche de fond — et **le serveur déjà en place continue de répondre**, donc les filets mesurent un autre arbre, parfois un autre dépôt. Parade, en deux temps : **① tout nouveau filet entre dans `livrer.sh` DANS LE COMMIT QUI LE CRÉE** — un filet qui n'est pas dans la parade n'existe pas ; **② le harnais s'éprouve au moins une fois depuis un état PROPRE, tous les processus manuels tués**, et il vérifie lui-même ses dépendances au lieu de les supposer : ici, que chaque serveur est vivant ET que ce qui sort du port est identique à l'octet au fichier local. Généralisation : **un harnais qui n'a jamais été exécuté ailleurs que là où il a été écrit n'a pas été testé, il a été observé.**
  - ⑰ **on DURCIT un relevé unique en contrainte, et la contrainte se met à voyager.** Distinct de ⑭, qui porte sur la FENÊTRE de mesure : ici la mesure est bien faite, elle n'est simplement faite QU'UNE FOIS. Vécu sur la vitrine : « l'élément LCP bascule de shot-desktop vers pitch dès 60 px de hauteur de ligne » — un seul relevé, aussitôt écrit comme « contrainte mesurée, pas préférence », repris tel quel dans la consigne suivante, puis inscrit en commentaire dans le code et en assertion dans le filet. **Refait n=3 par hauteur : à 60 px la bascule ne se produit pas. Le seuil réel est entre 100 et 110 px.** Le chiffre faux avait déjà traversé trois supports. **Ce n'est pas la relecture qui l'a corrigé, c'est le CONTRÔLE NÉGATIF** — écrit pour prouver que le filet savait voir la bascule, il a refusé de rougir, et c'est ce refus qui a fait reprendre la mesure. Deuxième fois en une semaine après ⑮, et des deux côtés de la conversation les deux fois. Parade : **un relevé unique s'énonce comme tel — « une observation, non reproduite » — et ne devient une contrainte, un commentaire ou une assertion qu'après n≥3.** Corollaire qui vaut à lui seul le cas : **le contrôle négatif n'est pas là pour valider le filet, il est là pour attraper le chiffre qui l'a fait écrire.** **RÉCIDIVE, 21/08/2026 — « facteur 7 ».** En chiffrant le chantier des captures, affirmation que `vitrine-grid.webp` fait « 1520 px de source pour ~220 px de rendu mobile, facteur 7 ». **Le chiffre comparait la LARGEUR de la source à la HAUTEUR du rendu**, et il est parti dans un message de commit et dans POINTS-SIGNALES avant d'être vérifié. Mesuré correctement — source contre rendu multiplié par la densité de pixels — le facteur vaut **2,2 à 390 px en ×2, 1,4 en ×3, et 1,0 sur le desktop en ×2**, où la source tombe juste au pixel près. Il n'y avait pas de gras à retirer ; le motif annoncé du chantier était faux. **LE COMPTE, parce qu'il finira par justifier autre chose qu'une parade** : c'est la TROISIÈME OCCURRENCE du motif — ⑰ à l'origine (« la bascule LCP à 60 px », n=1 durci en contrainte), puis la récidive de deux heures notée dans ⑱ (du bruit d'exécution transformé en tableau par fenêtre), puis celle-ci — donc la **deuxième récidive** si l'on ne compte pas l'origine. Les deux lectures sont données parce qu'un compte cité sans son périmètre est exactement la faute que ⑦ interdit. Ce qui compte est la tendance : **trois fois en une semaine, et les trois fois le chiffre a voyagé avant d'être vérifié.** Une parade qui échoue trois fois n'est plus une parade, c'est un rappel — la prochaine étape n'est pas de la réécrire mais d'en faire un MÉCANISME : un relevé unique ne devrait pas pouvoir entrer dans un commentaire, un message de commit ou une assertion sans porter, dans le texte même, la mention « n=1, non reproduit ».
  - ⑱ **le test est VERT sur un chemin jamais emprunté.** Famille distincte de ⑧ : là une ABSENCE était mal conclue ; ici c'est une PRÉSENCE — un vert — qui ne prouve rien, parce que la condition censée être éprouvée ne s'est jamais produite. Vécu sur la vitrine : le filet assertait « CLS < 0,1 » pour protéger une face de repli écrite contre l'échange de police… alors qu'en local le woff2 arrive AVANT le premier rendu (15-56 ms contre 20-76 ms de FCP). **Aucun échange, donc aucun décalage, donc un vert obtenu sans que le correctif ait jamais servi** — et un chiffre qui oscillait entre 0,0000 et 0,0035 selon lequel des deux gagnait la course. Parade : **pour tout garde écrit contre une condition, VÉRIFIER QUE LA CONDITION SE PRODUIT pendant le test** — ici, retarder la police pour que l'échange ait lieu ; ailleurs, provoquer la lenteur, la coupure, la langue longue. Un vert n'a de valeur que si l'on peut nommer ce qu'il a éprouvé. **CE QUI L'A DÉMASQUÉ MÉRITE D'ÊTRE RETENU** : le retard de police avait été posé pour rendre un CONTRÔLE NÉGATIF déterministe, et c'est lui qui a révélé que le passage NOMINAL en avait autant besoin. Un contrôle négatif ne vérifie pas seulement le garde : il peut découvrir un défaut dans le chemin nominal. **RÉCIDIVE DE ⑱ LUI-MÊME, deux jours après cette entrée :** le filet de la vitrine assertait le CLS à 390 et 1280, et rendait vert pendant qu'à **320 px de large le CTA sautait de 25 px en production** — la face de repli avait été calibrée à 375/390 et débordait en dessous. Vert, encore, sur un chemin jamais emprunté. **Une parade fraîche ne protège pas encore : elle ne devient un réflexe qu'après avoir été appliquée contre soi une fois** — ce qui était déjà écrit pour ⑰ ci-dessous, et vient de se reproduire mot pour mot sur ⑱. Le cas particulier qu'il faut en retenir a son propre numéro : ⑳. **Et une note d'humilité qui a sa place ici : ⑰ a été récidivé deux heures après avoir été écrit** — trois relevés uniques transformés en tableau « par fenêtre » alors que c'était du bruit. Une parade fraîche ne protège pas encore ; elle ne devient un réflexe qu'après l'avoir appliquée contre soi une fois.
  - ⑲ **une ATTENTE FIXE calibrée sur un cache chaud est un instrument qui ment DANS L'AUTRE SENS.** Miroir exact de ⑱ : là un vert imméritée, ici un ROUGE imméritée. Vécu sur `verify_qr.py` : les 900 ms fixes posées après `goto` suffisaient sur un poste au cache chaud et PAS sur un clone neuf — le graphe de modules n'était pas prêt, l'import dynamique de `backup.js` échouait, et la livraison rendait ROUGE pour une raison qui n'accusait pas le produit. Reproduit deux fois à froid, vert deux fois à chaud. **Un faux rouge use la confiance dans le garde aussi sûrement qu'un faux vert** — c'est lui qui rend un `; git push` tentant, et il a servi exactement à ça une fois. Parade : **dans un filet, on attend un SIGNAL (`wait_for_selector`), jamais une durée** ; toute durée fixe qui reste déclare sur quel état de cache elle a été calibrée.
  - ⑳ **le JEU DE FENÊTRES d'un filet est lui-même une hypothèse non vérifiée.** Cas particulier de ⑱, et assez fréquent pour mériter son numéro : le filet mesure correctement, avec le bon instrument, sur les bonnes métriques — mais l'ensemble des conditions qu'il balaie n'a JAMAIS été justifié. Vécu sur la vitrine : `FENETRES = 390 + 1280` depuis l'origine, sans qu'aucune mesure n'ait dit pourquoi ces deux-là. Le défaut vivait à **320 px**, la seule largeur où le paragraphe de repli repasse à six lignes ; le filet ne mentait pas, **il ne regardait pas là où le défaut habitait**. Même famille pour un contrôle négatif : celui qui prouvait que le filet savait voir la bascule LCP visait 360×640 ; le pitch raccourci de cinq lignes à trois y a repoussé le seuil au-delà de 150 px, et le contrôle a cessé de prouver quoi que ce soit **sans jamais devenir rouge**. Parade : **devant tout filet, poser la question « quelle condition ne teste-t-il pas ? » — et écrire à côté du jeu de conditions la raison de chacune.** Un balayage sans raison écrite est un périmètre par habitude. Corollaire : **un contrôle négatif se recalibre quand la page change**, sinon il rejoint la collection des verts qui ne prouvent rien.
  - ㉑ **j'accuse l'ENVIRONNEMENT avant d'avoir lu le message d'erreur.** C'est « soupçonner l'instrument avant le produit » retourné contre son auteur : la règle protège le produit de mes mesures, elle ne dit rien du cas où le fautif est MON code. Vécu sur un banc d'images qui échouait en boucle : premier réflexe, redémarrer le serveur de test — qui répondait 200 depuis le début. Le vrai message, quatre lignes plus bas dans la trace, disait `Route.fulfill: Object of type Request is not JSON serializable` : **Playwright appelle un gestionnaire de route avec `(route, request)`, et mon second paramètre à valeur par défaut se faisait donc écraser par la requête.** Trois exécutions perdues à soupçonner l'infrastructure d'un défaut que la trace nommait. Parade : **lire le message AVANT de formuler une hypothèse** — et quand la trace est longue, la filtrer sur les lignes qui ne sont pas du cadre (`grep -v '^  File'`) plutôt que de lire les dix dernières, qui sont presque toujours celles de la bibliothèque et presque jamais celles de la cause.
  - ㉒ **un CONTRÔLE NÉGATIF calibré sur une valeur du produit se périme quand le produit bouge — et il se périme EN VERT.** À ne pas confondre avec ⑳ : ⑳ dit qu'un filet peut ne pas regarder au bon endroit ; ici il regardait bien, **puis le bon endroit s'est déplacé sous lui**. Vécu sur la vitrine : le contrôle qui prouvait que le filet savait voir la bascule de l'élément LCP portait la ligne à 110 px et vérifiait la bascule **à 360×640**, où le seuil valait 107 ± 1 tant que le pitch faisait cinq lignes. Le pitch est passé à trois : le seuil y est parti **au-delà de 150 px**, et le contrôle a cessé de prouver quoi que ce soit — sans une seule ligne rouge, puisqu'un contrôle qui ne déclenche plus rend la campagne verte. **Le contrôle négatif est le seul garde que rien ne garde** : quand un test se périme, un contrôle négatif le dit ; quand c'est le contrôle qui se périme, personne ne le dit. Parade : **tout contrôle négatif calibré sur un seuil mesuré DÉCLARE ce seuil à côté de lui, et le seuil se revérifie dès que ce qu'il mesure bouge.** Corollaire pratique : un contrôle dont on ne sait pas dire sur quelle valeur du produit il s'appuie ne peut pas être revérifié — donc il faut l'écrire au moment où on l'écrit, pas le jour où on le soupçonne.
  - ㉓ **le harnais produit un tableau PLAUSIBLE, et c'est plus dangereux qu'un harnais qui plante.** Vécu en comparant quatre réglages d'un fond animé : les variantes étaient indexées par leur première lettre, et « **A** SOURD » comme « **A**CTUEL » donnent `A`. La capture de l'une a écrasé celle de l'autre, et le tableau de mesures a rendu des chiffres complets, cohérents, bien formatés — **et faux d'une ligne sur quatre**. Rien n'a levé d'exception, rien n'a manqué : un dictionnaire qui écrase une clé ne se plaint pas. **CE QUI L'A DÉMASQUÉ N'EST PAS UNE VÉRIFICATION, C'EST UNE REDONDANCE APPARENTE DANS LE RÉSULTAT** — deux lignes du tableau étaient identiques au centième près, ce qui n'arrive pas entre deux réglages différents. Parade, et elle est plus utile que « relire le code » : **se méfier d'un résultat qui SE RÉPÈTE.** Deux cellules identiques, deux exécutions au chiffre près, une colonne constante là où l'on faisait varier quelque chose : ce n'est presque jamais une belle stabilité, c'est presque toujours une donnée qui n'a pas voyagé jusqu'au bout. Corollaire d'écriture : **un identifiant de variante se dérive de son INDEX, jamais d'un fragment de son libellé** — un libellé est fait pour être lu par un humain, pas pour être unique. **DEUXIÈME CAS, 21/08/2026, et il porte la leçon générale.** Une sonde tactile rapportait que le glissé de fermeture d'une fiche ne marchait pas : événements reçus, dy=120 contre un seuil de 80, et la fiche restait ouverte. Trois mécanismes indépendants testés — le bouton de fermeture, un glissé de 60, un glissé de 120 — **et les trois rendaient `False`**. La sonde lisait `getComputedStyle(mo).display`, alors que `.mo` est `display:flex` EN PERMANENCE : l'ouverture se porte sur la classe `.open`. **Elle mesurait une constante.** Repris avec le bon prédicat : le bouton ferme, 60 px ne ferme pas, 120 px ferme — le produit n'avait rien. Trois `False` sur trois mécanismes indépendants, c'est la signature décrite juste au-dessus, disponible depuis deux jours et non appliquée. **CE QUI TRAVERSE ⑰, ⑱ ET ㉓ ET VAUT PLUS QUE LES TROIS CAS : une parade s'applique quand on CHERCHE une faute, pas quand on croit avoir TROUVÉ un résultat.** Les trois fois, l'auteur était en train de conclure — le chiffre était déjà écrit, la campagne déjà finie, la sonde avait déjà « répondu ». **C'est le moment où la vigilance baisse, et c'est exactement celui où la parade sert.** Parade opérationnelle : **relire ses parades AU MOMENT DE CONCLURE**, pas au moment de mesurer — un résultat n'est acquis qu'après être passé par le registre, et l'ordre naturel (mesurer, conclure, vérifier) est précisément l'inverse de celui qui protège. **ET L'AMPLIFICATION, parce qu'elle a un coût réel :** la sonde avait été rendue avec la mention « signal fort, pas preuve », et le chantier est malgré tout passé devant tout le reste au motif d'un « bug en production ». **Deuxième fois en une semaine** après la phrase du correctif CLS étendue à Android et Windows sans mesure (㉔). Un signal qualifié d'incomplet par celui qui le rend ne doit pas être requalifié en fait par celui qui le reçoit : **l'étiquette voyage avec le chiffre, ou elle ne sert à rien.**
  - ㉔ **UN CONTRÔLE NÉGATIF PROUVE CE QU'IL EXERCE, ET RIEN DE PLUS.** À distinguer nettement de ⑱ : là, un test rendait vert sur un chemin jamais emprunté ; **ici le contrôle rougissait bien, sur la bonne branche, et il était juste — c'est la GÉNÉRALISATION qu'on en a tirée qui était fausse.** Vécu sur la face de repli de la vitrine : un contrôle négatif éprouvait « `local()` introuvable → le CLS revient exactement à sa valeur d'avant correctif », et il le prouvait, n=3, variance nulle. De là est née une phrase écrite en ligne dans un dépôt public : **« le correctif ne peut jamais faire pire que de ne rien faire ».** Elle couvrait une branche que le contrôle n'avait jamais empruntée — celle où la face SE CHARGE, sur une police présente dont les chasses ne sont pas celles du calibrage. Mesurée après coup : **CLS 0,3598 contre 0,0063 sans aucun repli, cinquante-sept fois pire que de ne rien faire**, et quatre fois au-dessus du seuil « Poor ». La phrase avait de surcroît été ÉTENDUE à Android et Windows sans qu'aucun des deux n'ait été éprouvé — un contrôle passé sur un seul système avait fini par garantir quatre. Parade, en deux temps : **① quand un contrôle négatif fonde une affirmation, écrire l'affirmation dans les termes exacts de ce que le contrôle a exercé** — « si la face échoue à se charger, alors… », et non « le correctif ne peut jamais… » ; **② énumérer les branches qu'il n'a PAS prises**, à côté de lui, au moment où on l'écrit. Une garantie est une affirmation universelle ; un contrôle est une observation particulière. **Le passage de l'un à l'autre ne se fait jamais tout seul, et c'est là que la faute se loge.** Corollaire : une branche énumérée mais non éprouvée est un contrôle à écrire, pas une réserve à mentionner.
  - ㉕ **DANS UNE COMPARAISON, LA RÉFÉRENCE DOIT ÊTRE À ÉGALE DISTANCE DES DEUX TERMES.** À distinguer de ⑦, qui porte sur les PÉRIMÈTRES déclarés, et de ㉔, qui porte sur ce qu'un contrôle exerce : **ici les deux périmètres étaient identiques et le contrôle était juste. C'est la CHAÎNE DE PRODUCTION des termes qui était asymétrique.** Vécu sur les captures de la vitrine : tous les candidats de compression étaient encodés depuis le JPEG, et la référence de la mesure ÉTAIT ce JPEG — tandis que le fichier en place, lui, venait du PNG de capture. Il portait une génération de perte de plus, et la mesure la lui comptait comme un défaut de QUALITÉ. **Biais chiffré, même encodeur et même qualité, seule la source changeant : +0,5 dB sur la grille, +2,0 dB sur les badges** — soit, sur les badges, la totalité de l'écart annoncé en faveur du candidat. **Refait avec le vrai maître comme référence, le classement s'inverse** : l'encodeur en place redevenait le plus fidèle des trois. Parade : **avant de comparer, écrire la CHAÎNE COMPLÈTE de chaque terme jusqu'à la référence, et vérifier qu'aucun terme n'a la référence dans sa lignée.** Un candidat dérivé de la référence est avantagé par construction ; s'il n'existe pas de terme neutre, c'est la référence qu'il faut aller chercher — ici, reproduire la capture pour retrouver l'image en mémoire. **Et l'amplification, TROISIÈME de la semaine :** le rapport disait « strictement meilleur sur les deux métriques », la consigne a repris « gain sans contrepartie ». Les deux étaient faux, mais le second l'était davantage — et c'est celui qui a servi à décider.
  - ㉖ **UNE RÈGLE D'EXCLUSION ÉCRITE POUR UN CAS PARTICULIER DEVIENT UN PIÈGE POUR TOUT CE QUI ARRIVE APRÈS — et sa spécificité gonflée empêche de la contourner proprement.** Vécu sur la vitrine : pour passer le contenu du hero au-dessus du canvas de fond, une règle `body > *:not(#fond){position:relative;z-index:1}`. Elle fait son travail. Mais `:not(#fond)` lui donne **la spécificité d'un identifiant**, et elle s'applique à TOUT enfant direct de `body` — y compris à ceux qui n'existaient pas quand elle a été écrite. La surcouche d'intro, ajoutée des semaines plus tard en `position:fixed`, s'est retrouvée en `relative` : dans le flux, sans contenu propre puisque ses enfants sont absolus, donc **0×0 et invisible**. Aucune classe ne pouvait la reprendre — il aurait fallu un `!important` ou un identifiant, c'est-à-dire surenchérir. **La bonne correction n'est pas de forcer la spécificité mais d'ÉTENDRE L'EXCLUSION** — `:not(#fond):not(.intro)` — avec le pourquoi écrit à côté et la consigne pour la suivante : *toute surcouche plein écran ajoutée plus tard doit être exclue ici*. Une surenchère de spécificité se paie à chaque ajout ultérieur ; une liste d'exclusion commentée se lit. Parade : **une règle qui vise « tous les enfants sauf X » doit porter, à côté d'elle, la phrase qui dit à quoi elle sert et ce qu'il faudra y ajouter** — sinon elle est un piège silencieux dont la victime sera toujours du code écrit plus tard, par quelqu'un qui n'a pas lu cette ligne-là. **CE QUI L'A DÉMASQUÉE mérite d'être noté, parce que c'est ㉑ appliqué à temps** : deux hypothèses avaient été formulées sur la cause, aucune vérifiée. La troisième action a été de MESURER LA GÉOMÉTRIE — rectangle du conteneur, du canvas, `position` calculée — et la réponse est tombée en une lecture. Arrêter de théoriser et mesurer coûte moins cher que la deuxième hypothèse.
  - ㉗ **UN INSTRUMENT DE CAPTURE DOIT CADRER PLUS LARGE QUE L'OBJET QU'IL JUGE.** **Entrée neuve, et pas une variante de ⑩** — c'est délibéré : ⑩ dit qu'on a mesuré la BOÎTE au lieu du CONTENU, donc la mauvaise grandeur sur le bon objet. Ici la grandeur et l'objet étaient les bons ; c'est le CHAMP de l'instrument qui était plus étroit que l'objet, et ce qu'il a laissé dehors a été porté au débit du produit. Vécu sur la bande de vignettes de la séquence d'intro : les captures étaient recadrées à 540 px de haut alors que la zone de tracé va de y=236 à y=776 sur une fenêtre de 844. Chaque silhouette était coupée par le bas, et le verdict « illisible » qui en est sorti visait pour une bonne part le recadrage, pas le dessin. **Ce que ça a coûté : un plan a bien été corrigé sur la foi d'un défaut réel (le rapport pixels/points), mais un autre tour de mesure a été dépensé à chercher dans le produit ce qui manquait dans l'instrument.** Parade : **une capture qui sert à JUGER se prend en plein cadre ; un recadrage ne s'introduit qu'après avoir vérifié que la boîte de l'objet y entre en entier.** Et le signe qui doit alerter : un défaut uniforme sur TOUTES les formes à la fois — quand tout est mauvais de la même façon, c'est rarement toutes les formes qui ont le même défaut, c'est ce qu'elles ont en commun, et ce qu'elles ont en commun c'est l'instrument.
  - ㉘ **UNE VALEUR RECOPIÉE DEPUIS UNE TABLE EST UNE COPIE QUI NE SE MET PAS À JOUR, ET SON ÉCHEC EST MUET.** Ce n'est pas la duplication en général qui est visée — c'est le cas précis où **une table est la source d'une valeur et où un littéral la recopie ailleurs**. Vécu dans la séquence d'intro : le tableau des plans donne les bornes de chacun, et trois animations internes (le compteur du mur, les crans de rareté, le cycle des langues) partaient chacune d'un littéral en millisecondes recopié à la main depuis ce tableau. **Retirer un plan les a toutes décalées d'un coup.** Et rien n'a cassé : un compteur qui démarre 900 ms trop tard ne lève aucune exception, ne fait échouer aucun test, il se voit — ou pas, selon l'instant où on regarde. **CE QUI REND CE CAS PARTICULIÈREMENT COÛTEUX : le symptôme n'apparaît pas là où est la faute.** On voit « le compteur affiche un chiffre bizarre » et on va chercher dans le compteur, alors que la faute est dans une édition du tableau faite ailleurs, plus tôt. Parade : **une valeur qui vit dans une table se LIT depuis la table, même quand la lire coûte une fonction de trois lignes** — ici `debut(nom)`. Et quand une borne se compte depuis la FIN plutôt que depuis le début, la dériver de la fin : l'apothéose de la séquence valait « début du dernier plan + 1,5 s », donc raccourcir ce plan la poussait hors des vingt secondes ; elle vaut maintenant `DUREE - 500`. **CE QUI N'EST PAS VISÉ, et qu'il faut savoir distinguer** : deux instruments indépendants qui portent le même chiffre. Le seuil de saut `1,75 × base` figure dans `fps.js` et dans le garde de `intro.js` ; ce n'est pas une copie à supprimer, ce sont deux mesures qui ont chacune le droit d'évoluer seule. La règle porte sur **table → copie**, pas sur **coïncidence de valeur**.
  - ㉜ **UN GARDE QUI PLANTE EST UN GARDE QUI AUTORISE.** Un contrôle qui échoue AUTREMENT qu'en rendant son verdict ne rend pas « je ne sais pas » : il rend « rien à signaler », parce que rien ne le distingue d'un contrôle passé. **Trois occurrences le 22-23/08/2026, sur trois mécanismes différents — c'est une classe, pas un accident :** ① le hook `pre-commit` écrit avec des noms de variables accentués mourait sur « unbound variable » **avant** sa vérification, selon le shell qui l'exécute — un hook mort laisse passer exactement ce qu'il devait refuser ; ② le mode `--controle` de `verify_vitrine.py` levait un `ValueError` au milieu de sa course : les quatre premiers contrôles rendaient leurs rouges, les trois derniers n'étaient plus joués, et **rien ne distinguait « quatre contrôles rouges » de « sept contrôles rouges »** ; ③ le bilan de ces contrôles, écrit à la fin de `main()`, ne survivait pas à l'exception qu'il était précisément censé rendre visible — le garde disparaissait avec ce qu'il gardait. **Parade en trois temps : (a) un garde déclare COMBIEN de vérifications il a jouées, pas seulement leurs verdicts ; (b) ce décompte se rend dans un `finally` ou depuis l'appelant, jamais à la fin du chemin nominal ; (c) son contrôle négatif ne consiste pas à casser ce qu'il surveille, mais à casser LE CHEMIN D'EXÉCUTION DU GARDE LUI-MÊME.** C'est (c) qui a démasqué (b) du premier coup, alors que (a) venait d'être écrit et paraissait suffisant.
  - ㉙ **UNE PROPORTION SANS PLAFOND EST UN RÉGLAGE QUI SE DÉRÈGLE TOUT SEUL.** Proche de ㉘ mais distincte : là, une valeur était RECOPIÉE d'une table ; **ici la valeur est bien dérivée — et c'est la dérivation qui devient fausse hors de la plage où elle a été réglée.** Vécu sur la séquence d'intro : la transition entre deux plans valait « 56 % de la durée du plan ». Juste, et même joli, sur un plan de 1,3 s (730 ms de vol). Sur un plan porté à 2,6 s, la même règle prend **1,46 s** — si bien que la carte du plan « rarete » n'était assemblée qu'au TROISIÈME de ses cinq crans : les deux premiers se jouaient sur des particules encore en l'air. Parade : **une proportion s'écrit avec sa borne** — `Math.min((d1 - d0) * .56, 800)` — parce qu'une proportion exprime un rapport, jamais une limite physique, et que la limite existe quand même. **CE QUI L'A DÉMASQUÉE, ET QUI EST LA VRAIE LEÇON** : photographier CHAQUE ÉTAT SÉPARÉMENT, pas relire le code. Le code était correct à la lecture ; c'est la bande des cinq crans, tirée un par un, qui a montré la carte encore en vol. **Une proportion se vérifie AUX DEUX BOUTS de la plage où elle s'applique, jamais au milieu** — au milieu elle est juste par construction. Deux formes voisines à reconnaître : **deux littéraux qui doivent s'additionner à 1** (`(u - a*.20) / .80` — changer l'un sans l'autre ne casse rien, ça décale silencieusement) : n'en écrire qu'un et calculer l'autre. Et **une constante absolue posée en face d'une durée variable** (cinq crans de 350 ms dans un plan de 2 600 ms) : écrire la CONTRAINTE plutôt que sa solution — `(duree - T0) / nombre_de_crans`, ou pour un compteur qui doit être posé avant le milieu de son plan, `duree / 2 - 250`.
  - ㉚ **COMPTER AVANT D'ÉCRIRE, PUIS ÉCRIRE LE COMPTE D'AVANT — ⑦ commis dans la correction de ⑦.** Vécu le 22/08/2026 en corrigeant l'en-tête de POINTS-SIGNALES, qui annonçait « aucun point ouvert » sur un fichier qui en comptait huit. Pour justifier qu'aucun test ne peut vérifier l'en-tête, un comptage des titres barrés : `grep -c '^## ~~'` → **0**. Le motif était faux — les titres s'écrivent `## 35. ~~texte~~`, le barré vient APRÈS le numéro. Le vrai compte est 18 sur 39. Le chiffre faux est parti tel quel **dans l'en-tête que cette correction écrivait**, c'est-à-dire dans la phrase censée expliquer pourquoi les chiffres de ce fichier se périment. **La parade n'est pas de mieux grepper : c'est de recompter au moment d'écrire, sur le texte final, pas sur une mesure prise plus tôt dans la session.** Un compte mesuré à l'étape 2 et écrit à l'étape 7 a eu six occasions de cesser d'être vrai — ici, une seule a suffi : la mesure elle-même était fausse dès le départ, et rien entre les deux ne l'a rejouée.
  - ㉛ **UNE CONSIGNE DE REMPLACEMENT PONCTUEL SUR UN MOTIF NON COMPTÉ TRANSMET LA FAUTE À CELUI QUI L'EXÉCUTE.** Vécu le 22/08/2026 : « remplace la borne `ab89cb1` par `49164b5` dans le n°30 — **rien d'autre** ». Le motif apparaissait **cinq fois** dans l'entrée. Le remplacement en bloc a bien changé la borne, et il a aussi réécrit la phrase « Dernier relevé connu, `ab89cb1` : 1 issue, LOC 12 164, couverture 72 % » — qui attribuait désormais au commit du 22/08 des chiffres mesurés le 20/08. **Une affirmation fausse fabriquée en suivant l'instruction à la lettre.** Le « rien d'autre » disait l'intention et interdisait précisément le contrôle qui l'aurait servie. **La règle vaut pour le DONNEUR D'ORDRE, pas seulement pour l'exécutant : avant d'écrire « remplace X par Y », compter X — et si le compte dépasse 1, dire QUELLE occurrence.** L'exécutant garde son devoir de compter avant de remplacer ; mais une consigne qui présuppose l'unicité sans l'avoir vérifiée met les deux dans la même erreur, et le « rien d'autre » décourage justement celui qui exécute d'y regarder.
- **Les outils de VÉRIFICATION vivent hors dépôt — la règle zéro dépendance porte sur ce que l'app EMBARQUE.** Installés à la main, jamais dans `package.json` : **Playwright + Chromium** (captures, parcours, `verify_tutorial.py`), **cairosvg** (icônes), et depuis 1.71.0 **OpenCV** (`pip3 install --user opencv-python-headless numpy`) pour `verify_qr.py`. Chacun doit **dire clairement qu'il manque** et sortir en échec — jamais passer au vert en silence, jamais faire une demi-vérification.
- **UN DÉCODEUR DE VÉRIFICATION DOIT ÊTRE INDÉPENDANT DE L'ENCODEUR VÉRIFIÉ.** `verify_qr.py` décode avec OpenCV, qui n'a aucun lien avec `qrcodegen.js` : relire avec l'encodeur du dépôt ne prouverait que sa cohérence avec lui-même, jamais qu'un lecteur du monde réel y arrive. C'est tout l'argument du test — le jour où quelqu'un « simplifie » en réutilisant `qrcodegen` pour lire, il ne reste qu'un aller-retour dans la même bibliothèque et le filet ne prouve plus rien. Vécu : le QR de la fiche d'échange était **correct** (charge intacte une fois recadrée) et pourtant **illisible en usage réel** — aucun test d'encodage ne pouvait le voir.
- **Un banc doit rendre des données RÉALISTES, sinon il mesure son propre artefact.** Deux fois dans la même heure, sur `verify_qr.py` : ① une charge dégénérée (`'A'` répété 700 fois) produit de grandes zones uniformes qui empêchent le décodeur de TROUVER le code — le banc criait au défaut alors que le même QR avec un base64 varié se décodait ; ② une capture d'élément plus large que la fenêtre revient tronquée — le viewport doit suivre la taille de l'objet mesuré. Dans les deux cas le symptôme ressemblait à un défaut du produit. **Réflexe : avant de croire un banc qui accuse, lui faire rendre le cas RÉEL.** Même famille que la règle de densité du banc de lisibilité.
- **TOUTE commande dont on lit la sortie sans lire son CODE DE SORTIE ment par omission.** La règle n'est pas « pas de `| tail` » — c'est le cas particulier qui l'a fait payer deux fois (le pipe rend le code du DERNIER maillon) ; la troisième fois, c'est un `grep` sur la sortie de `npm test` qui a laissé une chaîne `&&` pousser un commit avec « # fail 1 » affiché à l'écran : le grep avait trouvé sa ligne, donc code 0, donc le commit est parti ROUGE (b164590). **Forme attendue, à chaque vérification qui conditionne une suite** : capturer d'abord (`cmd > f.log 2>&1; ec=$?`), décider sur `$ec`, lire `f.log` ensuite. Filtrer la sortie est un confort d'affichage ; le verdict, lui, est dans le code de sortie, et rien d'autre ne doit décider.
- **Avant d'attribuer une différence à un changement, vérifiez que la mesure est reproductible SANS changement.** Un refactor du seed de captures a produit 41 fichiers différents sur 52 : conclusion évidente, le refactor avait changé le rendu. Le contrôle — deux exécutions du **même** script, sans rien toucher — en a donné 42. Le script n'était pas déterministe à l'octet ; le refactor n'y était pour rien. Le réflexe coûte une exécution et évite une heure de recherche d'une cause qui n'existe pas. Il vaut pour toute mesure bruitée : captures, temps de rendu, taille de bundle.
- **Le bundle IIFE MASQUE les symboles oubliés lors d'un déplacement de code. `npm run lint` les attrape ; le navigateur reste nécessaire pour le RESTE.** Vécu pendant le découpage de `badges.js` : cinq symboles (`DAY_MS`, `autoBadgeUnlocked`, `_bt`, `badgeDifficulty`, `difficultyLabelKey`) sont restés dans le module d'origine alors que le code qui les utilise avait déménagé. `npm run build` passait, `npm test` passait — **955 tests verts** — parce qu'esbuild en `--format=iife` met tout dans une portée unique, où un symbole « manquant » reste visible. En modules bruts (`index-dev.html`), c'est un `ReferenceError` immédiat. **Depuis 1.60.2, `npm run lint` (règle Biome `correctness/noUndeclaredVariables`, bloquante en CI) détecte cette classe entière en 40 ms** — elle a trouvé deux des cinq du premier coup, que ni les tests ni une vérification navigateur manuelle n'avaient vus, parce que personne n'avait ouvert le sélecteur de titres. **Ce que le lint NE voit PAS, et qui garde donc sa vérification navigateur** : le rendu (rien ne dit qu'une modale s'affiche correctement), les chemins qu'aucun test n'emprunte et dont la panne n'est pas un symbole manquant, et tout ce qui dépend d'un état d'exécution. **Règle pratique : après un déplacement entre modules, `npm run lint` d'abord — c'est instantané et exhaustif sur sa classe — puis `index-dev.html` pour exercer ce qui se voit.**
- **Un déplacement de code produit DEUX erreurs symétriques, et un seul garde n'en voit qu'une.** Le premier garde (`noUndeclaredVariables`) attrape le symbole **utilisé sans être importé**. Il ne peut rien dire du symbole **importé sans être utilisé** — il regarde dans l'autre sens. Le découpage de `badges.js` a produit les deux : cinq du premier type, **dix-neuf du second**. Codacy en a signalé onze ; Biome a trouvé les huit autres. **Depuis 1.60.3, `npm run lint` arme les deux règles** (`noUndeclaredVariables` + `noUnusedImports`), toutes deux bloquantes en CI. **Pourquoi `noUnusedImports` et pas `noUnusedVariables`** : cette dernière compte 56 constats dont **54 sont des `catch(e)` muets** — une étape rouge en permanence apprend à ignorer le CI. Les imports, eux, sont à zéro : le garde peut être bloquant sans jamais mentir. Le reste du bruit reste sur l'étape informative. **Un import mort n'est pas cosmétique** : il maintient une arête dans le graphe d'imports. Les dix-neuf de `badges.js` retiraient à eux seuls l'arête `badges.js → storage.js` et **trois arêtes sur 128** (128 → 125), sans qu'une ligne de logique bouge.
- **Une valeur qui ressemble à un hack peut être une correction. Lire son commentaire avant de la retirer.** Vécu au recentrage du casque (1.63.1) : le bloc monogramme des cartes écurie porte un `translateY(-18px)` qui a toute l'allure d'une rustine d'alignement — c'est en fait la **parade minimale mesurée** à une collision entre le nom d'écurie (2 lignes) et le bouton « + » (débordement de +10 à +17 px si on centre, mesuré sur les 9 écuries). Le retirer « en même temps que le centrage », comme le prévoyait le plan, aurait réintroduit un bug déjà payé et documenté sur place. **Réflexe** : avant de supprimer une valeur étrange, lire le commentaire qui la porte — et s'il n'y en a pas, la mesurer ; la différence entre un hack et une correction, c'est qu'une correction a une raison qui tient encore.
- **Un banc de lisibilité doit rendre à la DENSITÉ où le problème se pose, pas à celle qui flatte le tracé.** Vécu au choix du casque (1.63.0) : le banc à densité 3 montrait qu'« aucun trait fin ne survit à 14 px » — bouillie nette, trois candidats éliminés. À **densité 1** — celle des écrans où le 14 px se pose réellement — l'anticrénelage fait FUSIONNER les traits en une masse qui garde le profil : le problème n'existait pas. L'instrument mentait avant la conclusion, dans les deux sens possibles : une densité haute préserve les détails (flatte un tracé fin, cache qu'une masse boucherait), une densité basse les fond. **Règle : rendre aux densités RÉELLES des contextes visés (1 ET 3), et juger chaque taille à la sienne.** Corollaire déjà payé ailleurs : les pixels se comparent grossis au plus proche voisin, jamais lissés. Même famille, payée au centrage du casque : `getBBox()` sur un `<use>` a rendu 0…120 sur un tracé qui n'occupe pas tout le cadre — quand l'instrument peut mentir, mesurer au RASTER (bornes et centroïde d'encre, pixel par pixel).
- **Un contrôle négatif qui passe au VERT doit être suspecté AVANT le garde.** Le contrôle obligatoire d'un garde est de le voir échouer (casser la chose protégée → rouge). Mais quand ce contrôle **passe au vert**, le réflexe est de conclure « le garde est mauvais » — et c'est souvent faux : c'est **la chaîne entre la modification et la vérification** qui est cassée. Vécu deux fois avec la même cause : `app.bundle.js` non reconstruit. ① Une vérification navigateur sur `index.html` validait un code que le bundle ne contenait pas encore ; ② un contrôle négatif de `verify_tutorial.py` (clé retirée de `tutorialKeys()`) est passé au vert parce que le script lit `index.html`, donc le bundle, et la modification n'y était pas. Même famille que le piège `--format=iife` : le bundle masque ce qu'on croit avoir changé. **Ordre de suspicion** : 1. la modification atteint-elle ce que la vérification lit (bundle reconstruit ? cache purgé ? bon fichier ?) — 2. le contrôle casse-t-il vraiment la chose protégée — 3. alors seulement, le garde. Variante de la même famille : un fragment cherché qui diffère par un caractère invisible (apostrophe typographique `’` contre ASCII `'`) fait tomber la vérification dans la mauvaise branche sans erreur.
- **Un garde-fou doit s'ancrer sur du CODE, jamais sur de la prose — il ignore les commentaires.** Vécu **deux fois en une session**, ce qui en fait un motif et non un accident. Un test cherchait `exception when others then return new` dans `db/03-functions.sql` : il passait au vert **après suppression du bloc qu'il protège**, parce qu'il trouvait la formule dans le **commentaire qui la décrit** juste au-dessus. Même famille : un test de markup satisfait par le commentaire d'exemple placé à côté. La doc d'un fichier cite toujours ce que le fichier fait — c'est même sa raison d'être — donc chercher une formule « quelque part dans le fichier » revient à vérifier que la documentation existe. **Parade** : filtrer les lignes de commentaire avant de chercher (`.split('\n').filter(l => !l.trimStart().startsWith('--'))`), et ancrer le motif (`/^…/m`) plutôt que de le laisser flotter. **Contrôle obligatoire** : supprimer le code visé et vérifier que le test devient rouge. Un garde qu'on n'a pas vu échouer n'a pas été vérifié, il a été espéré.
- **Ne recliquez pas la navigation pour « forcer » l'affichage d'une vue.** Les transitions entre vues (1.32.0) ont une durée ; un clic pendant la transition la **relance depuis le début**. Un script d'automatisation qui cliquait sur l'onglet Stats toutes les 200 ms tant que la vue n'apparaissait pas ne l'a **jamais** fait apparaître — chaque clic annulait le précédent, et le symptôme (« la vue ne s'affiche pas ») ressemble à une panne, pas à un excès de zèle. **Un seul clic, puis attendre** (~1,5 s suffit ; poller l'apparition du contenu, pas recliquer).
- **Ne sondez pas les modules bruts depuis `index.html`.** Un harnais qui fait `await import('./cloud-sync.js')` dans la page **de production** charge une **SECONDE copie** du graphe de modules — la page tourne déjà sur `app.bundle.js` — et la ré-exécution d'`app.js` casse sur une initialisation circulaire (`Cannot access '_authenticated' before initialization`). Le symptôme ressemble à un défaut du produit ; ce n'en est pas un, personne ne charge le bundle ET les modules bruts ensemble. **Sonder depuis `index-dev.html`**, où `import()` rend les mêmes instances que la page. Corollaire : le SEED de `capture_screenshots.py` injecte une **session cloud fictive** (`f1uno_cloud_session`) pour les captures — la retirer du contexte avant tout test cloud réel, sinon elle masque la vraie session.
- **Arc browser** hides behind a Chrome UA and has no install support; `install.js` detects it via `--arc-palette-*` CSS vars and shows an honest message. Don't "simplify" that away.
- **Driver-number fonts** (Orbitron / Racing Sans One) are intentionally fixed and must **not** follow the font-theme picker.
- **Colour/contrast:** stat and rarity palettes are contrast-checked in **both** light and dark themes. If you change chart/tile/rarity colours, re-check contrast in both themes (the `dataviz` skill provides a palette-contrast validator; there is no such script committed in this repo).

---

## 10. When unsure / En cas de doute

Ask the maintainer before: adding any dependency or CDN; changing the rarity scales, the storage key scheme, or the migration; altering the service-worker caching strategy; or making an irreversible data change. Prefer a small, tested, reversible change over a clever large one. Preserve the honest, precise tone of the existing code and docs.

🇫🇷 Demander avant : toute dépendance/CDN, tout changement des échelles de rareté, du schéma de clés `localStorage`, de la migration, ou de la stratégie de cache du service worker. Préférer une petite modif testée et réversible. Conserver le ton honnête et précis du code et de la doc existants.
